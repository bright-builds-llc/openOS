---
phase: 24-notes-structured-model-and-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/notes/notesContent.ts
  - src/features/apps/notes/notesContent.test.ts
  - src/features/apps/notes/notesModel.ts
  - src/features/apps/notes/notesModel.test.ts
autonomous: true
requirements: [NOTES-01, NOTES-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-05-28T03-12-00
generated_at: 2026-05-28T03:29:16Z
must_haves:
  truths:
    - "Structured paragraph, heading, and checklist item text stays searchable and previewable through the Notes model helpers."
    - "Existing plain textarea text can be converted into a structured paragraph document without adding editor controls."
    - "Search and previews read structured content text, including heading and checklist labels."
  artifacts:
    - path: "src/features/apps/notes/notesContent.ts"
      provides: "Pure structured content types, parser, and plain-text adapter helpers"
      exports: ["NOTE_CONTENT_DOCUMENT_VERSION", "NoteContentDocument", "NoteContentBlock", "createNoteContentFromPlainText", "getNoteContentText", "parseNoteContentDocument"]
    - path: "src/features/apps/notes/notesContent.test.ts"
      provides: "Focused coverage for structured content helpers"
    - path: "src/features/apps/notes/notesModel.ts"
      provides: "Note type updated to canonical structured content plus search/preview helpers"
      exports: ["Note", "NoteInput", "filterNotes", "getNotePreview", "getNoteBodyText"]
    - path: "src/features/apps/notes/notesModel.test.ts"
      provides: "Focused coverage for structured search, preview, title, and folder behavior"
  key_links:
    - from: "src/features/apps/notes/notesModel.ts"
      to: "src/features/apps/notes/notesContent.ts"
      via: "filterNotes/getNotePreview/getNoteBodyText import and use pure content helpers"
      pattern: "getNoteContentText\\(note\\.content\\)"
    - from: "src/features/apps/notes/notesContent.test.ts"
      to: "src/features/apps/notes/notesContent.ts"
      via: "tests assert typed paragraph, heading, checklist, and invalid parser behavior"
      pattern: "checklistItem"
---

<objective>
Create the structured Notes content contract and move pure search/preview behavior onto it.

Purpose: Phase 24 needs a canonical structured local model before storage migration can normalize durable payloads into it.
Output: `notesContent.ts` plus updated model helpers and focused unit coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
@.planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
@src/features/apps/notes/notesModel.ts
@src/features/apps/notes/notesModel.test.ts
@package.json

<interfaces>
Existing model surface before this plan:
```typescript
export type Note = {
  id: string;
  title: string;
  body: string;
  folderId: string;
  createdAt: string;
  updatedAt: string;
};

export type NoteInput = {
  title: string;
  body: string;
  folderId: string;
};

export function filterNotes(
  maybeNotes: Note[],
  options: { query: string; maybeFolderId: string | null },
): Note[];
```

Contract this plan must create for downstream plans:
```typescript
export const NOTE_CONTENT_DOCUMENT_VERSION = 1;

export type NoteContentBlock =
  | { kind: "paragraph"; text: string }
  | { kind: "heading"; text: string }
  | { kind: "checklistItem"; text: string; checked: boolean };

export type NoteContentDocument = {
  version: typeof NOTE_CONTENT_DOCUMENT_VERSION;
  blocks: NoteContentBlock[];
};

export function createNoteContentFromPlainText(body: string): NoteContentDocument;
export function getNoteContentText(content: NoteContentDocument): string;
export function parseNoteContentDocument(maybeValue: unknown): NoteContentDocument | null;
export function getNoteBodyText(note: Note): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure structured content helpers</name>
  <files>src/features/apps/notes/notesContent.ts, src/features/apps/notes/notesContent.test.ts</files>
  <read_first>
    - src/features/apps/notes/notesContent.ts (create; read if present)
    - src/features/apps/notes/notesContent.test.ts (create; read if present)
    - .planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
    - .planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
    - src/features/apps/notes/notesModel.ts
    - src/features/apps/notes/notesModel.test.ts
    - package.json
  </read_first>
  <behavior>
    - `createNoteContentFromPlainText("Line one\nLine two")` returns `{ version: 1, blocks: [{ kind: "paragraph", text: "Line one\nLine two" }] }`.
    - `createNoteContentFromPlainText("")` returns `{ version: 1, blocks: [] }`.
    - `getNoteContentText` joins paragraph, heading, and checklist item text with `\n`.
    - `parseNoteContentDocument` accepts only `version: 1`, `blocks: []`, `kind: "paragraph" | "heading" | "checklistItem"`, string `text`, and boolean `checked` for checklist items.
  </behavior>
  <action>
    Create `src/features/apps/notes/notesContent.ts` with plain TypeScript data and pure helpers per D-01, D-02, D-03, D-04, and D-12. Use `kind` as the discriminant, not optional fields. Export `NOTE_CONTENT_DOCUMENT_VERSION = 1`, `NoteContentBlock`, `NoteContentDocument`, `createEmptyNoteContent`, `createNoteContentFromPlainText`, `getNoteContentText`, and `parseNoteContentDocument`. Do not add a rich-text editor package, HTML parsing, markdown parsing, runtime schema dependency, storage calls, React imports, `innerHTML`, or `dangerouslySetInnerHTML`.

    Add `src/features/apps/notes/notesContent.test.ts` with Vitest tests using `// Arrange`, `// Act`, and `// Assert`. Cover paragraph conversion, empty conversion, mixed paragraph/heading/checklist extraction, invalid document version rejection, invalid block kind rejection, and checklist blocks missing `checked` rejection.
  </action>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesContent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "export const NOTE_CONTENT_DOCUMENT_VERSION = 1" src/features/apps/notes/notesContent.ts` finds the exported content document version.
    - `rg -n "kind: \"paragraph\"|kind: \"heading\"|kind: \"checklistItem\"" src/features/apps/notes/notesContent.ts` finds all three block variants.
    - `rg -n "createNoteContentFromPlainText|getNoteContentText|parseNoteContentDocument" src/features/apps/notes/notesContent.ts` finds all required pure helpers.
    - `rg -n "dangerouslySetInnerHTML|innerHTML|localStorage|window\\." src/features/apps/notes/notesContent.ts` returns no matches.
    - `bun run test -- src/features/apps/notes/notesContent.test.ts` passes.
  </acceptance_criteria>
  <done>Structured content helpers exist, support paragraph/heading/checklist text, reject invalid raw documents, and remain pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move search and previews to structured content</name>
  <files>src/features/apps/notes/notesModel.ts, src/features/apps/notes/notesModel.test.ts</files>
  <read_first>
    - src/features/apps/notes/notesContent.ts
    - src/features/apps/notes/notesContent.test.ts
    - src/features/apps/notes/notesModel.ts
    - src/features/apps/notes/notesModel.test.ts
    - .planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
    - .planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
  </read_first>
  <behavior>
    - `Note` has `content: NoteContentDocument` and no canonical durable `body` field.
    - `NoteInput` keeps `body: string` for the current plain textarea adapter surface.
    - `filterNotes` matches all query terms case-insensitively across `note.title` and `getNoteContentText(note.content)`.
    - `getNotePreview` collapses structured content text whitespace and returns `Empty note` when content text is blank.
    - `getNoteBodyText(note)` returns `getNoteContentText(note.content)` for the existing textarea value path.
  </behavior>
  <action>
    Update `src/features/apps/notes/notesModel.ts` so `Note` imports `NoteContentDocument` from `./notesContent` and stores `content` as the canonical note text per D-01. Keep `NoteInput` with `title`, `body`, and `folderId` per D-03 so storage/UI callers can pass plain textarea values. Add `getNoteBodyText(note: Note): string` for the UI adapter. Change `getNotePreview` and `filterNotes` to call `getNoteContentText(note.content)` per D-09 and D-10 while preserving existing all-term, case-insensitive search semantics and folder filtering. Do not add storage parsing or migration logic to this file.

    Update `src/features/apps/notes/notesModel.test.ts` fixtures to use `createNoteContentFromPlainText` or explicit structured content. Add tests proving a heading term and checklist item label are searchable, multiple query terms still must all match, previews come from checklist/heading text, empty structured content returns `Empty note`, and default-folder sorting behavior still passes.
  </action>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesContent.test.ts src/features/apps/notes/notesModel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "content: NoteContentDocument" src/features/apps/notes/notesModel.ts` finds the canonical content field.
    - `rg -n "body: string" src/features/apps/notes/notesModel.ts` finds `body` only on `NoteInput`, not on `Note`.
    - `rg -n "getNoteContentText\\(note\\.content\\)" src/features/apps/notes/notesModel.ts` finds structured extraction in search/preview/body adapter paths.
    - `rg -n "checklistItem|heading" src/features/apps/notes/notesModel.test.ts` finds structured search/preview coverage.
    - `bun run test -- src/features/apps/notes/notesContent.test.ts src/features/apps/notes/notesModel.test.ts` passes.
  </acceptance_criteria>
  <done>Notes pure model helpers consume structured content while the plain body input contract remains available for the current Notes app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw content document -> `notesContent` parser | Future storage and tests may provide untrusted JSON-shaped content documents. |
| Structured content text -> React render/search strings | User-authored text must remain text data, not markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `parseNoteContentDocument` | mitigate | Strictly accept only `version: 1`, array `blocks`, known discriminants, string text, and boolean checklist `checked`; return `null` for invalid shapes. |
| T-24-02 | Tampering | `NoteContentBlock` model | mitigate | Use a discriminated union with `kind` so paragraph, heading, and checklist states cannot be mixed through optional fields. |
| T-24-03 | Information Disclosure | `notesContent` / `notesModel` | accept | Phase 24 adds no network, account, sync, or external persistence path; content helpers remain pure local data transforms. |
| T-24-04 | Elevation of Privilege | structured note text rendering | mitigate | Do not introduce HTML rendering, `innerHTML`, `dangerouslySetInnerHTML`, markdown parsing, or editor framework output in this plan. |
</threat_model>

<verification>
Run the focused pure-helper suite:

```bash
bun run test -- src/features/apps/notes/notesContent.test.ts src/features/apps/notes/notesModel.test.ts
```
</verification>

<success_criteria>
- `notesContent.ts` defines a typed versioned content document with paragraph, heading, and checklist item blocks.
- `notesModel.ts` stores canonical note text as `content`, not durable `body`.
- Search and preview helpers read structured text and preserve the existing case-insensitive all-term matching behavior.
- No storage, React, browser, or editor-package logic is added to the pure content module.
</success_criteria>

<output>
After completion, create `.planning/phases/24-notes-structured-model-and-migration/24-01-SUMMARY.md`
</output>
