---
phase: 13-managed-browser-app
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/browser/browserDestinations.ts
  - src/features/apps/browser/BrowserFrame.tsx
  - src/features/apps/browser/browserFrame.css
  - public/browser-fixtures/embed-safe.html
  - src/features/apps/browser/browserDestinations.test.ts
autonomous: true
requirements:
  - BROW-02
  - BROW-03
user_setup: []
must_haves:
  truths:
    - The Browser app is driven by an explicit destination model rather than arbitrary free-form URL browsing.
    - The managed iframe host can render embed-safe destinations inside the app surface.
    - The codebase has a deterministic fallback path for destinations that should open externally instead of trying to embed them.
  artifacts:
    - A typed Browser destination registry exists.
    - A managed iframe host component exists.
    - Deterministic embed-safe fixture content exists for verification.
  key_links:
    - Browser destination metadata explicitly controls embedded vs fallback behavior.
    - The iframe host is isolated to the Browser phase instead of leaking generic iframe behavior into the rest of the runtime.
    - Verification can target stable fixture destinations rather than relying on third-party sites.
---

<objective>
Build the Browser destination model and managed iframe host.

Purpose: Establish a truthful Browser foundation around curated destinations and a contained iframe host before any Browser app UI is added.
Output: Typed browser destination registry, managed iframe host, deterministic fixture content, and focused destination coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-managed-browser-app/13-RESEARCH.md
@src/features/platform/appDefinitions.ts
@src/features/runtime/appRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a typed Browser destination registry</name>
  <files>src/features/apps/browser/browserDestinations.ts, src/features/apps/browser/browserDestinations.test.ts</files>
  <action>Create a typed Browser destination model for the curated destinations supported in `v1.1`, including explicit metadata that determines whether a destination is embedded or shown through graceful fallback. Keep the model narrow and truthful.</action>
  <verify>`pnpm test` passes with focused coverage for destination registry behavior.</verify>
  <done>The Browser phase has a stable metadata source for curated navigation and fallback handling.</done>
</task>

<task type="auto">
  <name>Task 2: Build the managed iframe host component</name>
  <files>src/features/apps/browser/BrowserFrame.tsx, src/features/apps/browser/browserFrame.css</files>
  <action>Create a managed iframe host component for embed-safe destinations. Keep it constrained, visually coherent with the existing shell, and explicit about loading/embedded state without pretending to introspect cross-origin content.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass with the new Browser host component in place.</verify>
  <done>The Browser phase has a dedicated iframe host rather than generic ad hoc iframe usage.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic embed-safe fixture content for verification</name>
  <files>public/browser-fixtures/embed-safe.html, src/features/apps/browser/browserDestinations.ts</files>
  <action>Create one or more deterministic local fixture destinations that are safe to embed and suitable for browser verification. Use them to keep the Browser phase testable without depending on external sites remaining embeddable forever.</action>
  <verify>`pnpm build` passes and the embed-safe fixture is reachable through the destination registry.</verify>
  <done>The Browser phase has a stable embedded destination for deterministic testing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser destination behavior is metadata-driven and truthful
- The iframe host and fixture content are ready for Browser app UI and verification
</success_criteria>

<output>
After completion, create `.planning/phases/13-managed-browser-app/13-01-SUMMARY.md`
</output>
