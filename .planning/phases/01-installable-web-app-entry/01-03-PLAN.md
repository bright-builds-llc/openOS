---
phase: 01-installable-web-app-entry
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - index.html
  - public/manifest.webmanifest
  - public/icons/openos-icon.svg
  - public/icons/openos-icon-180.png
  - public/icons/openos-icon-512.png
  - src/features/install/standalone/StandaloneEntry.tsx
  - src/features/install/standalone/LaunchState.ts
  - src/features/install/standalone/LaunchState.test.ts
  - src/features/install/standalone/LaunchStateStorage.ts
  - src/features/install/standalone/standaloneEntry.css
autonomous: true
requirements:
  - INST-02
user_setup: []
must_haves:
  truths:
    - Installed launches enter a standalone-specific entry path rather than the browser-mode onboarding flow.
    - The installed app has the correct app identity metadata for `openOS`.
    - First launch shows only a very short app-like launch state, while returning launches enter directly.
  artifacts:
    - `manifest.webmanifest` exists with openOS identity metadata.
    - Original icon assets exist and are wired into the app metadata.
    - Standalone launch-state logic exists with unit coverage and persistence for first-run vs returning behavior.
  key_links:
    - `index.html` references the manifest and Apple web-app metadata.
    - StandaloneEntry uses the launch-state logic instead of hard-coded timers or one-off branches.
---

<objective>
Implement the installed app identity and standalone entry behavior for openOS.

Purpose: Fulfill the “launch from the Home Screen into standalone app mode” requirement with the approved restrained branding and short first-launch feel.
Output: Manifest and icon metadata, Apple web-app metadata, and a standalone entry surface that distinguishes first launch from returning launches.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-installable-web-app-entry/01-CONTEXT.md
@.planning/phases/01-installable-web-app-entry/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire manifest and Apple web-app metadata for openOS identity</name>
  <files>index.html, public/manifest.webmanifest, public/icons/openos-icon.svg, public/icons/openos-icon-180.png, public/icons/openos-icon-512.png</files>
  <action>Create the explicit app identity metadata for the installed experience: app name, standalone display, portrait orientation, and original icon assets. Keep the identity restrained and original. Prefer a hand-authored manifest and direct `index.html` metadata so the implementation stays understandable while Phase 1 is still learning platform edge cases.</action>
  <verify>Build the app and inspect the generated HTML/manifest output paths; confirm referenced icon files exist.</verify>
  <done>The app serves install identity metadata and original icon assets appropriate for an iPhone-installed web app.</done>
</task>

<task type="auto">
  <name>Task 2: Implement standalone launch-state behavior</name>
  <files>src/features/install/standalone/StandaloneEntry.tsx, src/features/install/standalone/LaunchState.ts, src/features/install/standalone/LaunchState.test.ts, src/features/install/standalone/LaunchStateStorage.ts, src/features/install/standalone/standaloneEntry.css</files>
  <action>Build the standalone entry surface so first launch shows a very short app-like launch state before entering the product, while returning launches skip repeated intro behavior. Keep the first-run vs returning decision in a pure, testable launch-state module backed by a minimal storage adapter.</action>
  <verify>`pnpm test -- --run LaunchState` passes and local standalone-branch behavior matches the approved first-run vs returning flow.</verify>
  <done>Installed entry behavior is restrained, app-like, and consistent with the approved identity decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Keep browser and standalone paths cleanly separated</name>
  <files>src/features/install/standalone/StandaloneEntry.tsx, index.html</files>
  <action>Ensure the standalone path does not inherit browser-mode guidance or preview affordances. The standalone entry surface should feel like the real product path, even though Phase 1 stops before the full shell work of Phase 2.</action>
  <verify>Run the local app using the dev-only standalone override introduced by Plan 01, confirm the standalone entry skips browser onboarding, then run `pnpm build`.</verify>
  <done>The installed path feels distinct from browser mode and is ready for the Phase 2 shell work that follows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run` passes for launch-state logic
- [ ] `pnpm build` passes
- [ ] Manifest, icons, and Apple metadata are present and referenced correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Installed mode enters a standalone-specific experience
- First-run and repeat-entry behavior matches the approved context
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-web-app-entry/01-03-SUMMARY.md`
</output>
