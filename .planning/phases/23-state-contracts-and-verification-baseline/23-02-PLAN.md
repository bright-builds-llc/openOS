---
phase: 23-state-contracts-and-verification-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/features/apps/notes/notesStorage.test.ts
  - scripts/verify-v1.3.sh
  - package.json
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-05-27T21-23-33
generated_at: 2026-05-27T21:40:27Z
must_haves:
  truths:
    - "Malformed or reset Notes app-session state does not delete durable Notes data."
    - "The v1.3 verification command exists and includes the focused state-contract tests."
    - "Storage failure and reset behavior stay covered by the repo-owned verification path."
  artifacts:
    - path: "src/features/apps/notes/notesStorage.test.ts"
      provides: "Durable Notes isolation regression for session reset"
    - path: "scripts/verify-v1.3.sh"
      provides: "Canonical v1.3 verification command script"
    - path: "package.json"
      provides: "`verify:v1.3` package script"
  key_links:
    - from: "src/features/apps/notes/notesStorage.test.ts"
      to: "src/features/platform/appSessionStorage.ts"
      via: "readAppSessionSnapshot/resetAppSessionSnapshot"
      pattern: "readAppSessionSnapshot"
    - from: "package.json"
      to: "scripts/verify-v1.3.sh"
      via: "verify:v1.3 script"
      pattern: "bash scripts/verify-v1.3.sh"
    - from: "scripts/verify-v1.3.sh"
      to: "src/features/platform/appSessionStorage.test.ts"
      via: "focused state-contract test command"
      pattern: "appSessionStorage.test.ts"
---

<objective>
Prove durable Notes data is isolated from disposable session resets and add the canonical v1.3 verification command.

Purpose: Phase 23 is only complete if malformed or reset app-session state cannot delete user-authored Notes data, and maintainers have one command that exercises the new state-contract baseline.
Output: Notes durable-isolation regression, `scripts/verify-v1.3.sh`, and `package.json` script wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/features/apps/notes/notesStorage.ts
@src/features/apps/notes/notesStorage.test.ts
@src/features/platform/appStorage.ts
@src/features/platform/appSessionStorage.ts
@scripts/verify-v1.2.sh
@package.json

<interfaces>
Existing durable Notes storage uses:
```typescript
const NOTES_STORAGE_KEY = "notes";
function getNotesStorageKey(namespace: string): string {
  return createAppStorageKey(namespace, NOTES_STORAGE_KEY);
}
```

Plan 01 creates these app-session helpers:
```typescript
export function readAppSessionSnapshot<TSession>(
  storage: AppSessionStorageLike,
  namespace: string,
  options: {
    version: number;
    defaultSession: TSession;
    parseSession: AppSessionPayloadParser<TSession>;
  },
): AppSessionReadResult<TSession>;

export function resetAppSessionSnapshot(
  storage: AppSessionStorageLike,
  namespace: string,
): AppSessionResetResult;
```
</interfaces>

Locked decisions implemented here: D-02, D-03, D-07, D-10, D-11. This plan depends on Plan 01 for the shared app-session helper. Do not implement Notes v3 migration, rich editor UI, Browser tabs, submitted registry generation, or Library/Calculator visible resume behavior.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable Notes isolation regressions for session reset</name>
  <files>src/features/apps/notes/notesStorage.test.ts</files>
  <read_first>
    - .planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
    - .planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
    - src/features/apps/notes/notesStorage.ts
    - src/features/apps/notes/notesStorage.test.ts
    - src/features/platform/appStorage.ts
    - src/features/platform/appSessionStorage.ts
  </read_first>
  <behavior>
    - A malformed `openos.apps.notes.session` snapshot returns reset reason `malformed-json`.
    - Resetting malformed Notes session data writes the default session snapshot only to `openos.apps.notes.session`.
    - Existing durable Notes data at `openos.apps.notes.notes` remains readable through `listStoredNotes`.
    - Explicit `resetAppSessionSnapshot(storage, namespace)` removes `openos.apps.notes.session` and leaves `openos.apps.notes.notes` untouched.
  </behavior>
  <action>Update `src/features/apps/notes/notesStorage.test.ts` with focused regression tests that import `createAppSessionStorageKey`, `readAppSessionSnapshot`, and `resetAppSessionSnapshot`. Define a local test-only Notes session shape such as `{ selectedFolderId: string; selectedNoteId: string | null }`, a default session `{ selectedFolderId: DEFAULT_NOTES_FOLDER_ID, selectedNoteId: null }`, and a local `parseNotesSession` function that returns the session only when `selectedFolderId` is a string and `selectedNoteId` is a string or null. Add one test that creates a durable note through `createStoredNote`, writes malformed JSON to `createAppSessionStorageKey(namespace)`, calls `readAppSessionSnapshot(storage, namespace, { version: 1, defaultSession, parseSession: parseNotesSession })`, and asserts `result.status === "reset"`, `result.reason === "malformed-json"`, the durable key `createAppStorageKey(namespace, "notes")` is still non-null, and `listStoredNotes(storage, namespace).map((note) => note.id)` is exactly `["note-1"]`. Add a second test that writes both a valid durable note and a valid session snapshot, calls `resetAppSessionSnapshot(storage, namespace)`, and asserts the session key is null while the durable note remains readable. Keep Arrange/Act/Assert comments in both tests. Do not change `notesStorage.ts`, migrate the Notes model, or add visible resume UI per D-02, D-03, and the deferred scope.</action>
  <acceptance_criteria>
    - `src/features/apps/notes/notesStorage.test.ts` imports `createAppSessionStorageKey` from `../../platform/appStorage`.
    - `src/features/apps/notes/notesStorage.test.ts` imports `readAppSessionSnapshot` and `resetAppSessionSnapshot` from `../../platform/appSessionStorage`.
    - The test file contains exact strings `openos.apps.notes.notes` or `createAppStorageKey(namespace, "notes")` and `openos.apps.notes.session` or `createAppSessionStorageKey(namespace)`.
    - One test asserts `result.status` is exactly `reset` and `result.reason` is exactly `malformed-json`.
    - One test asserts `storage.getItem(createAppSessionStorageKey(namespace))` is null after `resetAppSessionSnapshot`.
    - The durable note id `note-1` remains returned by `listStoredNotes(storage, namespace)` after both malformed-read reset and explicit session reset.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesStorage.test.ts</automated>
  </verify>
  <done>Durable Notes data is regression-tested against malformed session reset and exact-key session reset.</done>
</task>

<task type="auto">
  <name>Task 2: Add the v1.3 verification command with focused state-contract tests</name>
  <files>scripts/verify-v1.3.sh, package.json</files>
  <read_first>
    - .planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
    - .planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
    - scripts/verify-v1.2.sh
    - package.json
    - src/features/platform/appStorage.test.ts
    - src/features/platform/appSessionStorage.test.ts
    - src/features/runtime/appRegistry.test.ts
    - src/features/apps/notes/notesStorage.test.ts
  </read_first>
  <action>Create `scripts/verify-v1.3.sh` modeled after `scripts/verify-v1.2.sh` with `#!/usr/bin/env bash`, `set -euo pipefail`, and breadcrumb `echo` lines prefixed with `[verify:v1.3]`. The command sequence must be: `bun run submissions:check`; focused state-contract tests with `bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appSessionStorage.test.ts src/features/runtime/appRegistry.test.ts src/features/apps/notes/notesStorage.test.ts`; full `bun run test`; `bun x tsc --noEmit`; `bun run build`; and `bun run test:e2e --project=webkit-iphone`. Update `package.json` scripts by adding `"verify:v1.3": "bash scripts/verify-v1.3.sh"` without removing or changing `verify:v1.2`. Do not add Python, new npm packages, a new global state manager, or any CI-only assumptions per D-09 and D-10.</action>
  <acceptance_criteria>
    - `scripts/verify-v1.3.sh` exists and starts with `#!/usr/bin/env bash`.
    - `scripts/verify-v1.3.sh` contains `set -euo pipefail`.
    - `scripts/verify-v1.3.sh` contains the exact focused command `bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appSessionStorage.test.ts src/features/runtime/appRegistry.test.ts src/features/apps/notes/notesStorage.test.ts`.
    - `scripts/verify-v1.3.sh` contains `bun run submissions:check`, `bun run test`, `bun x tsc --noEmit`, `bun run build`, and `bun run test:e2e --project=webkit-iphone`.
    - `package.json` contains `"verify:v1.3": "bash scripts/verify-v1.3.sh"` and still contains `"verify:v1.2": "bash scripts/verify-v1.2.sh"`.
  </acceptance_criteria>
  <verify>
    <automated>bun run verify:v1.3</automated>
  </verify>
  <done>The repo exposes a canonical `verify:v1.3` command that includes the focused state-contract tests and the existing milestone-quality checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notes durable storage -> session reset helper | Durable user-authored Notes data must not be touched by disposable session reset behavior. |
| Maintainer CLI -> verification surface | The repo-owned verification command must actually exercise the state-contract tests it claims to cover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering / Information Loss | `notesStorage.test.ts` session reset regression | mitigate | Add tests that corrupt or remove only `openos.apps.notes.session` and prove `openos.apps.notes.notes` remains readable. |
| T-23-06 | Repudiation / Denial of Service | `scripts/verify-v1.3.sh` | mitigate | Include focused app-session tests in the repo-owned command before full test/typecheck/build/E2E checks so state-contract regressions fail visibly. |
| T-23-07 | Information Disclosure | Notes session test payload | accept | Test-only session payload contains selection ids only; no secrets, auth tokens, sync credentials, or account data are introduced in Phase 23. |
</threat_model>

<verification>
Run the focused durable isolation check:
`bun run test -- src/features/apps/notes/notesStorage.test.ts`

Run the new canonical milestone command:
`bun run verify:v1.3`
</verification>

<success_criteria>
- Malformed Notes session data resets without deleting durable Notes data.
- Exact session reset removes `openos.apps.notes.session` only.
- `package.json` exposes `verify:v1.3`.
- `verify:v1.3` runs focused state-contract tests before the full existing verification surface.
- Existing `verify:v1.2` remains available.
</success_criteria>

<output>
After completion, create `.planning/phases/23-state-contracts-and-verification-baseline/23-02-SUMMARY.md`.
</output>
