---
phase: 08-preview-install-ux-parity-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/features/install/browser/BrowserInstallFlow.tsx
  - src/features/install/browser/BrowserInstallOverlay.tsx
  - src/features/install/browser/AppTapIntercept.tsx
  - src/features/install/browser/browserInstall.css
autonomous: true
requirements:
  - INST-01
user_setup: []
must_haves:
  truths:
    - Browser-mode install CTA buttons now perform an assistive action instead of acting as decoration only.
    - The assist behavior is truthful for the iPhone/Safari install path and does not pretend to trigger a native install prompt.
    - Overlay and intercept install CTAs share one consistent assist behavior.
  artifacts:
    - Browser install flow exposes one install-assist path that overlay/intercept components can reuse.
    - Overlay and intercept primary buttons are wired to that shared assist behavior.
    - Any visual emphasis or focus treatment required by the assist flow is implemented in browser install styling.
  key_links:
    - CTA assist behavior flows through BrowserInstallFlow rather than being duplicated in individual components.
    - Tapping `Install openOS` increases the visibility/actionability of the Safari-specific steps.
    - The assist behavior remains compatible with the preview/install-state model already in BrowserInstallFlow.
---

<objective>
Make the browser install CTAs meaningfully assistive.

Purpose: Replace the current decorative `Install openOS` buttons with one shared, truthful install-assist behavior that helps users perform the Safari install flow.
Output: Shared install-assist action in BrowserInstallFlow, wired overlay/intercept CTAs, and focused browser install polish for the assist state.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/08-preview-install-ux-parity-cleanup/08-RESEARCH.md
@src/features/install/browser/BrowserInstallFlow.tsx
@src/features/install/browser/BrowserInstallOverlay.tsx
@src/features/install/browser/AppTapIntercept.tsx
@src/features/install/browser/installSteps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define one shared install-assist action in BrowserInstallFlow</name>
  <files>src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Create a single browser-install assist path that can be invoked from both the full takeover overlay and the app-tap intercept. The action should truthfully help the user perform the Safari install flow by reopening or emphasizing the install guidance, rather than simulating a native install prompt.</action>
  <verify>The browser install flow still builds and the assist action can be invoked from both browser entry states.</verify>
  <done>The browser install flow owns one consistent install-assist behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire overlay and intercept CTAs to the shared install-assist behavior</name>
  <files>src/features/install/browser/BrowserInstallOverlay.tsx, src/features/install/browser/AppTapIntercept.tsx, src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Connect the primary `Install openOS` buttons to the shared assist action and keep the CTA semantics consistent between the takeover and the preview intercept. Avoid broad copy rewrites; the goal is behavior, not messaging churn.</action>
  <verify>Local browser-mode interaction or targeted browser automation confirms the primary install CTA now performs an assistive action in both surfaces.</verify>
  <done>The primary browser install CTA is no longer decorative-only.</done>
</task>

<task type="auto">
  <name>Task 3: Add the minimal browser-install polish needed to make the assist action feel intentional</name>
  <files>src/features/install/browser/browserInstall.css, src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Add only the small visual or focus treatment needed so the install-assist action feels responsive and clearly directs the user to the Safari-specific install steps. Keep this narrowly scoped to the browser install surface.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the install-assist polish.</verify>
  <done>The browser install CTA feels intentional and helpful without pretending to automate installation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Targeted browser interaction confirms CTA assist behavior works from overlay and intercept
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser install CTAs perform truthful assist behavior
- Overlay and intercept CTAs stay behaviorally aligned
</success_criteria>

<output>
After completion, create `.planning/phases/08-preview-install-ux-parity-cleanup/08-02-SUMMARY.md`
</output>
