---
phase: 21-submitted-manifest-discovery-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "21-01"
files_modified:
  - scripts/validate-submitted-apps.ts
  - src/features/platform/submittedAppManifests.ts
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-04-11T21-45-01
generated_at: 2026-04-11T21:45:01.035Z
must_haves:
  truths:
    - `bun run submissions:check` fails when manifest files on disk and the registered manifest set diverge.
    - The validator still performs the existing content validation for registered manifests after the drift check passes.
    - The hardened validator produces actionable output naming the specific drift problem.
  artifacts:
    - The validator scans `src/features/platform/submitted-apps/` for JSON files before validating manifest content.
    - Drift-check failures identify unregistered files and missing registered files separately.
    - Existing successful validation output still works when no drift exists.
  key_links:
    - The validator consumes the shared drift-detection helper from the submission module instead of duplicating comparison logic.
    - The validator remains the canonical repo-native entrypoint for submission review.
    - On-disk manifest additions can no longer silently bypass validation and catalog inclusion.
---

<objective>
Harden the submitted manifest validator to fail closed on registry drift.

Purpose: Turn the new drift-detection model into a real repo safety gate so `bun run submissions:check` blocks any manifest file mismatch before the catalog can drift from the reviewed source set.
Output: Fail-closed validator behavior and actionable CLI output for drift issues.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-CONTEXT.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-RESEARCH.md
@src/features/platform/submittedAppManifests.ts
@scripts/validate-submitted-apps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan submitted manifest files before content validation</name>
  <files>scripts/validate-submitted-apps.ts</files>
  <read_first>
    - scripts/validate-submitted-apps.ts
    - src/features/platform/submittedAppManifests.ts
    - docs/app-submissions.md
  </read_first>
  <action>Update `scripts/validate-submitted-apps.ts` to read the JSON filenames from `src/features/platform/submitted-apps/` using Bun/Node filesystem APIs before running the existing manifest-content validation. Pass those discovered filenames into the shared drift-detection helper and stop immediately with a non-zero exit code if drift is found.</action>
  <acceptance_criteria>
    - `scripts/validate-submitted-apps.ts` reads `src/features/platform/submitted-apps/`
    - `scripts/validate-submitted-apps.ts` exits with `process.exit(1)` when drift issues are present
    - `scripts/validate-submitted-apps.ts` continues to existing content validation only after the drift check passes
  </acceptance_criteria>
  <verify>`bun run submissions:check` passes in the current clean repo state.</verify>
  <done>The repo-native validator checks on-disk manifest discovery before content validation.</done>
</task>

<task type="auto">
  <name>Task 2: Make validator drift failures actionable</name>
  <files>scripts/validate-submitted-apps.ts</files>
  <read_first>
    - scripts/validate-submitted-apps.ts
    - .planning/v1.2-v1.2-MILESTONE-AUDIT.md
    - docs/app-submissions.md
  </read_first>
  <action>Extend the validator output so it prints specific labels for `unregistered_on_disk` and `registered_missing_file` cases, naming the exact JSON filenames or manifest ids involved. Preserve the existing success output: `Validated N submitted app manifest(s).` and the catalog-ready count when no drift exists.</action>
  <acceptance_criteria>
    - `scripts/validate-submitted-apps.ts` contains output text for unregistered on-disk manifest files
    - `scripts/validate-submitted-apps.ts` contains output text for registered manifests missing backing files
    - `scripts/validate-submitted-apps.ts` still contains `Validated ${validationResults.length} submitted app manifest(s).`
  </acceptance_criteria>
  <verify>`bun run submissions:check` prints the existing success summary when no drift exists.</verify>
  <done>Contributors can diagnose manifest drift from CLI output without reading implementation code.</done>
</task>

</tasks>

<threat_model>
Primary risk: the validator reports success even though newly added manifest files are not part of the catalog source, creating false confidence in distribution review.
Mitigation required: scan on-disk manifests, compare against the shared registry, and fail before content validation whenever drift exists.
Block on: any validator path that returns success while on-disk manifests and the registered manifest set are out of sync.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bun run submissions:check` passes
- [ ] `bun x tsc --noEmit` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The validator fails closed on manifest-registry drift
- Contributors get actionable drift output instead of silent success
</success_criteria>

<output>
After completion, create `.planning/phases/21-submitted-manifest-discovery-hardening/21-02-SUMMARY.md`
</output>
