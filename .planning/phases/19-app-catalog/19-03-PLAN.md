---
phase: 19-app-catalog
plan: "03"
type: execute
wave: 3
depends_on:
  - "19-02"
files_modified:
  - tests/e2e/app-catalog.spec.ts
  - tests/e2e/app-integration.spec.ts
  - tests/e2e/settings.spec.ts
  - src/features/platform/appSettings.test.ts
  - src/features/platform/appDefinitions.test.ts
  - src/features/runtime/appRegistry.test.ts
autonomous: true
requirements:
  - PLAT-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-11T19-00-30
generated_at: 2026-04-11T19:00:30.974Z
must_haves:
  truths:
    - Browser-path verification proves the in-product catalog opens through the real launcher path.
    - Browser-path verification proves catalog entries and detail panels are driven by shared submission metadata.
    - Existing integrated shell flows still hold after `Library` becomes a real app surface.
  artifacts:
    - A dedicated catalog Playwright scenario exists.
    - Integration/settings/runtime tests reflect the new implemented Library/catalog surface.
    - Final repo verification passes with the catalog changes in place.
  key_links:
    - Launcher-path verification proves the real catalog app rather than isolated component rendering.
    - The integrated shell tests prove Library, Browser, Notes, and Settings still work together on page 1.
    - Phase 20 can reuse the catalog launcher-path checks as milestone-level evidence.
---

<objective>
Verify the in-product app catalog through the real launcher path.

Purpose: Lock the new catalog surface behind browser-path proof so browse/detail behavior and shell integration are trusted before final milestone verification.
Output: Catalog browser verification plus final catalog stabilization.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-app-catalog/19-CONTEXT.md
@src/features/apps/catalog/AppCatalogApp.tsx
@src/features/platform/submittedAppManifests.ts
@tests/e2e/fixtures/launcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launcher-path catalog verification</name>
  <files>tests/e2e/app-catalog.spec.ts</files>
  <action>Add a focused Playwright scenario that opens Library through the launcher, browses categories, switches entries, and verifies detail content and review-repository links from shared metadata.</action>
  <verify>`bun run test:e2e -- tests/e2e/app-catalog.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The catalog browse/detail flow is proven through the real launcher/runtime path.</done>
</task>

<task type="auto">
  <name>Task 2: Update integration expectations for the real Library surface</name>
  <files>tests/e2e/app-integration.spec.ts, tests/e2e/settings.spec.ts, src/features/platform/appSettings.test.ts, src/features/platform/appDefinitions.test.ts, src/features/runtime/appRegistry.test.ts</files>
  <action>Update integration and platform tests so they reflect Library as a real implemented app and settings-visible built-in surface.</action>
  <verify>The integrated shell and platform test suites pass after the Library promotion.</verify>
  <done>The rest of the repo recognizes the catalog as a first-class built-in app.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final Phase 19 verification set</name>
  <files>tests/e2e/app-catalog.spec.ts, src/features/apps/catalog/AppCatalogApp.tsx</files>
  <action>Run the repo validation set that covers the catalog model, catalog UI, launcher-path catalog verification, and cross-app regression behavior, making only small stabilization fixes if the issue is clearly within Phase 19 scope.</action>
  <verify>`bun run submissions:check`, `bun run test`, `bun x tsc --noEmit`, `bun run build`, and `bun run test:e2e --project=webkit-iphone` all pass.</verify>
  <done>The app catalog is ready for final milestone verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run submissions:check` passes
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
- [ ] `bun run test:e2e --project=webkit-iphone` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The first in-product catalog is launcher-path verified and metadata-driven
- The integrated shell remains stable after Library becomes a real app
</success_criteria>

<output>
After completion, create `.planning/phases/19-app-catalog/19-03-SUMMARY.md`
</output>
