---
phase: 08-preview-install-ux-parity-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/install/browser/PreviewShell.tsx
  - src/features/install/browser/browserPreviewApps.ts
  - src/features/install/browser/browserPreviewApps.test.ts
  - src/features/install/browser/browserInstall.css
autonomous: true
requirements:
  - HOME-02
  - HOME-03
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - The browser preview no longer hardcodes its app inventory inline in the component.
    - Preview app content is derived from the shared runtime registry through a thin browser-preview selector.
    - The preview remains intentionally lighter-weight than the real launcher rather than becoming a second runtime implementation.
  artifacts:
    - A browser preview selector module exists.
    - Preview selector coverage exists if pure selection logic is introduced.
    - PreviewShell renders from the shared preview selector output.
  key_links:
    - Preview app labels come from the runtime app registry instead of duplicated string constants.
    - The preview selector is curated for browser mode but grounded in the same app identity source as the real launcher.
    - Preview-shell rendering remains compatible with the existing browser install flow.
---

<objective>
Align the browser preview inventory with the real launcher.

Purpose: Remove preview/runtime drift by deriving browser preview content from the runtime app registry through a thin shared selector.
Output: Shared browser preview app selector, PreviewShell wired to it, and focused coverage protecting the selector behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/08-preview-install-ux-parity-cleanup/08-RESEARCH.md
@src/features/install/browser/PreviewShell.tsx
@src/features/install/browser/BrowserInstallFlow.tsx
@src/features/runtime/appRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a thin shared browser-preview selector over the runtime registry</name>
  <files>src/features/install/browser/browserPreviewApps.ts, src/features/install/browser/browserPreviewApps.test.ts</files>
  <action>Create a browser-preview data module that derives a curated preview grid and dock inventory from the runtime app registry rather than hardcoded labels in the component. Keep it intentionally thin and browser-specific so preview mode shares identity provenance without becoming a second launcher runtime.</action>
  <verify>`pnpm test` passes with focused coverage for the preview selector behavior.</verify>
  <done>The browser preview now has a shared, testable data source tied to the real app registry.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PreviewShell to the shared preview selector output</name>
  <files>src/features/install/browser/PreviewShell.tsx, src/features/install/browser/browserInstall.css</files>
  <action>Replace the inline preview icon array with rendering driven by the new preview selector. Preserve the phase-appropriate preview feel while using real app metadata for labels and any light icon/glyph treatment that improves parity without overbuilding the preview shell.</action>
  <verify>`pnpm build` passes and the preview still renders coherently in browser mode.</verify>
  <done>The preview shell stays visually intentional while no longer drifting from the real launcher inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Keep preview interactions compatible with the existing browser install flow</name>
  <files>src/features/install/browser/PreviewShell.tsx, src/features/install/browser/BrowserInstallFlow.tsx</files>
  <action>Ensure the shared preview selector preserves the current preview tap behavior and browser-install flow contract. Do not change the meaning of preview taps in this plan; only keep the new shared data path compatible with the existing install intercept behavior.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the preview selector integration.</verify>
  <done>Preview parity improves without regressing the browser install flow behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser preview inventory is derived from the shared runtime registry
- Preview mode remains intentionally lighter than the full launcher
</success_criteria>

<output>
After completion, create `.planning/phases/08-preview-install-ux-parity-cleanup/08-01-SUMMARY.md`
</output>
