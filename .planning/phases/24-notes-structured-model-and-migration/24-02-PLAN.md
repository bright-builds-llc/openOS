---
phase: 24-notes-structured-model-and-migration
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/features/apps/notes/notesStorage.ts
  - src/features/apps/notes/notesStorage.test.ts
autonomous: true
requirements: [NOTES-01, NOTES-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-05-28T03-12-00
generated_at: 2026-05-28T03:29:16Z
must_haves:
  truths:
    - "Existing version-2 Notes snapshots migrate into version 3 structured notes without losing title, body text, folder, or timestamps."
    - "Legacy flat note arrays still import into the same version 3 structured model."
    - "Folder normalization remains unchanged after migration."
    - "Malformed durable Notes payloads fall back safely without deleting unrelated session or catalog state."
  artifacts:
    - path: "src/features/apps/notes/notesStorage.ts"
      provides: "Durable Notes version 3 parser, normalizer, migration, and write path"
      contains: "NOTES_STORAGE_VERSION = 3"
    - path: "src/features/apps/notes/notesStorage.test.ts"
      provides: "Focused migration, write-path, folder, and durable/session isolation regressions"
  key_links:
    - from: "src/features/apps/notes/notesStorage.ts"
      to: "src/features/apps/notes/notesContent.ts"
      via: "version-2 body and legacy array body conversion through `createNoteContentFromPlainText`"
      pattern: "createNoteContentFromPlainText\\("
    - from: "src/features/apps/notes/notesStorage.ts"
      to: "src/features/platform/appStorage.ts"
      via: "durable key remains `createAppStorageKey(namespace, \"notes\")`"
      pattern: "createAppStorageKey\\(namespace, NOTES_STORAGE_KEY\\)"
---

<objective>
Migrate durable Notes storage to the structured version 3 snapshot while preserving existing local notes.

Purpose: Users with current local Notes data must reopen their notes after Phase 24 with title, body text, folder assignment, timestamps, searchability, and previews intact.
Output: `notesStorage.ts` parses version 3, version 2, and legacy array payloads into one structured model and writes only version 3 snapshots.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
@.planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-01-SUMMARY.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-02-SUMMARY.md
@src/features/apps/notes/notesContent.ts
@src/features/apps/notes/notesModel.ts
@src/features/apps/notes/notesStorage.ts
@src/features/apps/notes/notesStorage.test.ts
@src/features/platform/appStorage.ts
@src/features/platform/appSessionStorage.ts

<interfaces>
Use the interface created by Plan 24-01:
```typescript
export type Note = {
  id: string;
  title: string;
  content: NoteContentDocument;
  folderId: string;
  createdAt: string;
  updatedAt: string;
};

export type NoteInput = {
  title: string;
  body: string;
  folderId: string;
};

export function createNoteContentFromPlainText(body: string): NoteContentDocument;
export function parseNoteContentDocument(maybeValue: unknown): NoteContentDocument | null;
```

Current durable key contract from Phase 23:
```typescript
createAppStorageKey(createAppStorageNamespace("notes"), "notes")
// => "openos.apps.notes.notes"

createAppSessionStorageKey(createAppStorageNamespace("notes"))
// => "openos.apps.notes.session"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement version 3 migration parser and write path</name>
  <files>src/features/apps/notes/notesStorage.ts, src/features/apps/notes/notesStorage.test.ts</files>
  <read_first>
    - src/features/apps/notes/notesContent.ts
    - src/features/apps/notes/notesModel.ts
    - src/features/apps/notes/notesStorage.ts
    - src/features/apps/notes/notesStorage.test.ts
    - .planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
    - .planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
  </read_first>
  <behavior>
    - A stored `{ version: 2, folders, notes: [{ id, title, body, folderId, createdAt, updatedAt }] }` payload returns a `Note` whose `content` is `createNoteContentFromPlainText(body)`.
    - A legacy flat array payload returns structured notes in the default folder.
    - A stored `{ version: 3, folders, notes: [{ id, title, content, folderId, createdAt, updatedAt }] }` payload returns structured notes unchanged after normalization.
    - Duplicate folders/notes are de-duped and notes with missing folders fall back to `DEFAULT_NOTES_FOLDER_ID`.
    - `NOTES_STORAGE_VERSION` is `3`.
    - `parseSnapshot` accepts version-3 snapshots, version-2 snapshots, and legacy flat arrays.
    - Version-2 and legacy notes convert `body` through `createNoteContentFromPlainText`.
    - Version-3 notes require a valid parsed `content` document and do not require durable `body`.
    - `writeSnapshot` serializes version-3 snapshots only.
  </behavior>
  <action>
    Update `src/features/apps/notes/notesStorage.test.ts` first. Add tests for version-2 snapshot migration, legacy flat-array migration into structured content, native version-3 snapshot parsing, duplicate folder/note de-dupe, missing-folder fallback, timestamp preservation, title/body text preservation, and version-3 write output per D-01, D-05, D-06, D-07, and D-14. Use `getNoteBodyText` or `getNoteContentText` in assertions instead of expecting `note.body`. Keep Arrange/Act/Assert comments in each new test.

    Then update `src/features/apps/notes/notesStorage.ts` per D-01, D-05, D-06, D-07, D-08, D-12, and D-13. Set `NOTES_STORAGE_VERSION = 3`. Split raw note parsing into `parseStoredNoteV3` and `parseStoredNoteV2` private helpers. Keep `StorageLike`, `getNotesStorageKey`, `readSnapshot`, `writeSnapshot`, and public CRUD function names unchanged. Parse raw storage only in `parseSnapshot`; public callers should receive typed `Note` objects with `content`.

    Keep the durable key as `createAppStorageKey(namespace, "notes")`. Do not call `createAppSessionStorageKey`, `resetAppSessionSnapshot`, `localStorage.clear`, or remove any key except the exact durable Notes key when the normalized snapshot is empty. Keep default folder insertion, duplicate folder/note de-dupe, sorting, and missing-folder fallback behavior unchanged. Convert `createStoredNote` and `updateStoredNote` body inputs by writing `content: createNoteContentFromPlainText(input.body)` or `content: createNoteContentFromPlainText(updates.body)` only when `updates.body` is provided.
  </action>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesStorage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "const NOTES_STORAGE_VERSION = 3" src/features/apps/notes/notesStorage.ts` finds the version bump.
    - `rg -n "parseStoredNoteV3|parseStoredNoteV2|parseLegacySnapshot" src/features/apps/notes/notesStorage.ts` finds distinct parser paths.
    - `rg -n "createNoteContentFromPlainText\\(" src/features/apps/notes/notesStorage.ts` finds version-2/legacy/create/update body conversion.
    - `rg -n "createAppSessionStorageKey|resetAppSessionSnapshot|localStorage\\.clear|\\.clear\\(" src/features/apps/notes/notesStorage.ts` returns no matches.
    - `rg -n "version: 2|version: 3|legacy" src/features/apps/notes/notesStorage.test.ts` finds explicit migration fixtures.
    - `rg -n "getNoteBodyText|getNoteContentText|createNoteContentFromPlainText" src/features/apps/notes/notesStorage.test.ts` finds structured content assertions.
    - `bun run test -- src/features/apps/notes/notesStorage.test.ts` passes.
  </acceptance_criteria>
  <done>Durable Notes reads normalize valid old and new payloads into version 3 structured notes, and writes persist version 3 only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve durable/session isolation regressions</name>
  <files>src/features/apps/notes/notesStorage.test.ts</files>
  <read_first>
    - src/features/apps/notes/notesStorage.ts
    - src/features/apps/notes/notesStorage.test.ts
    - src/features/platform/appStorage.ts
    - src/features/platform/appSessionStorage.ts
    - .planning/phases/23-state-contracts-and-verification-baseline/23-02-SUMMARY.md
  </read_first>
  <behavior>
    - Malformed durable Notes JSON returns an empty Notes collection without removing `openos.apps.notes.session` or unrelated catalog-like keys.
    - Malformed Notes session JSON still resets only session state and leaves version-3 durable notes readable.
    - Explicit `resetAppSessionSnapshot` still removes only `openos.apps.notes.session`.
  </behavior>
  <action>
    Extend the existing Phase 23 durable/session isolation tests in `notesStorage.test.ts` so their durable Notes fixture is a version-3 structured note. Add one explicit malformed durable Notes payload test that seeds `openos.apps.notes.session` and an unrelated key such as `openos.apps.catalog.reviewed`, calls `listStoredNotes`, and asserts the unrelated keys are still present. This closes D-08 and D-13 without adding Notes resume/session behavior.
  </action>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesStorage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "openos\\.apps\\.catalog\\.reviewed|createAppSessionStorageKey" src/features/apps/notes/notesStorage.test.ts` finds unrelated-key and session-key assertions.
    - `rg -n "malformed durable|invalid stored payloads|ignores invalid stored payloads" src/features/apps/notes/notesStorage.test.ts` finds durable malformed-payload coverage.
    - `bun run test -- src/features/apps/notes/notesStorage.test.ts` passes.
  </acceptance_criteria>
  <done>Storage regression coverage proves version 3 migration does not weaken the Phase 23 durable/session safety boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` string -> Notes snapshot parser | Durable Notes payloads are user/device-controlled JSON and must be treated as untrusted. |
| Legacy/version-2 note body -> structured content | Old plain text must become typed content without losing data or becoming markup. |
| Notes durable key -> app-session/catalog keys | Migration must not delete or mutate unrelated app data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | `parseSnapshot` | mitigate | Parse version-3, version-2, and legacy arrays as explicit variants; invalid payloads return an empty Notes snapshot only. |
| T-24-06 | Tampering | `writeSnapshot` | mitigate | Keep writes on `createAppStorageKey(namespace, "notes")` and serialize normalized `version: 3` snapshots only. |
| T-24-07 | Denial of Service | malformed durable payload | mitigate | Wrap `JSON.parse` in the existing catch path and do not throw into the app shell for malformed Notes data. |
| T-24-08 | Tampering | durable/session key boundary | mitigate | Tests assert malformed durable payloads and session resets do not remove `openos.apps.notes.session` or unrelated app keys. |
| T-24-09 | Information Disclosure | storage migration | accept | Migration remains origin-local and introduces no network, logging, sync, account, or export path. |
</threat_model>

<verification>
Run the focused storage suite:

```bash
bun run test -- src/features/apps/notes/notesStorage.test.ts
```
</verification>

<success_criteria>
- Existing version-2 snapshots and legacy arrays read back as structured version-3 notes.
- Title, body text, folder assignment, `createdAt`, and `updatedAt` are preserved.
- Version-3 writes do not include a canonical durable `body` field on notes.
- Invalid durable Notes payload handling remains safe and does not clear unrelated session/catalog state.
</success_criteria>

<output>
After completion, create `.planning/phases/24-notes-structured-model-and-migration/24-02-SUMMARY.md`
</output>
