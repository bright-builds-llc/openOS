---
phase: 12-notes-app
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/notes/notesModel.ts
  - src/features/apps/notes/notesStorage.ts
  - src/features/apps/notes/notesStorage.test.ts
  - src/features/platform/appStorage.ts
autonomous: true
requirements:
  - NOTE-02
  - NOTE-03
user_setup: []
must_haves:
  truths:
    - Notes persistence is handled through a dedicated notes repository/storage adapter rather than ad hoc component state.
    - Notes local persistence uses the shared app-storage namespace conventions from Phase 10.
    - The notes domain and repository behavior are testable without rendering the UI.
  artifacts:
    - A notes domain model exists.
    - A notes storage/repository module exists.
    - Focused unit coverage exists for note persistence behavior.
  key_links:
    - Notes storage is anchored to the shared storage namespace convention rather than an unrelated local key.
    - The Notes UI phase can consume repository helpers instead of owning persistence logic itself.
    - Notes persistence is isolated from Settings and install/launch storage helpers.
---

<objective>
Build the Notes domain and local persistence layer.

Purpose: Establish a dedicated notes repository and persistence path before the Notes UI lands, so Notes behavior is testable and grounded in the shared storage conventions from Phase 10.
Output: Notes model, notes repository/storage adapter, and focused persistence tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-notes-app/12-RESEARCH.md
@src/features/platform/appStorage.ts
@src/features/platform/appDefinitions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Notes domain model</name>
  <files>src/features/apps/notes/notesModel.ts</files>
  <action>Create a small notes domain model appropriate for local-only CRUD: note identity, content shape, timestamps, and any small helper functions needed for list/editor behavior. Keep it plain and phase-appropriate.</action>
  <verify>`npx tsc --noEmit` passes with the new model in place.</verify>
  <done>The Notes feature has a stable domain shape before the UI is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Create a dedicated Notes repository/storage adapter</name>
  <files>src/features/apps/notes/notesStorage.ts, src/features/platform/appStorage.ts</files>
  <action>Create a local Notes repository/storage adapter that uses the shared app-storage namespace convention. Keep the adapter narrow and deterministic so later UI work can call into it without owning persistence itself.</action>
  <verify>`pnpm test` passes with the notes repository module added.</verify>
  <done>Notes persistence is anchored to a dedicated repository backed by the platform storage convention.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused persistence coverage for Notes</name>
  <files>src/features/apps/notes/notesStorage.test.ts</files>
  <action>Add unit coverage for create/update/delete/list/reopen behavior and any key local-persistence edge cases. Keep the tests centered on repository behavior, not UI rendering.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The Notes persistence layer is ready for the app UI phase and protected against basic regression.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Notes has a dedicated local-only repository layer
- Persistence uses the shared storage namespace convention
</success_criteria>

<output>
After completion, create `.planning/phases/12-notes-app/12-01-SUMMARY.md`
</output>
