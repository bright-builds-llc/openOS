---
phase: 19-app-catalog
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/submittedAppManifests.ts
  - src/features/platform/submittedAppManifests.test.ts
  - src/features/platform/submitted-apps/signal-box.json
  - src/features/apps/catalog/appCatalogModel.ts
  - src/features/apps/catalog/appCatalogModel.test.ts
autonomous: true
requirements:
  - PLAT-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-11T19-00-30
generated_at: 2026-04-11T19:00:30.974Z
must_haves:
  truths:
    - The catalog reads only validated catalog-ready submission metadata.
    - The catalog has enough sample data to support real browse behavior instead of a single-entry placeholder.
    - Category browsing semantics are deterministic and testable without rendering the UI.
  artifacts:
    - Catalog selectors exist for category listing and category filtering.
    - At least two valid catalog-ready submissions exist in the shared metadata source.
    - Focused unit coverage exists for the catalog selector behavior.
  key_links:
    - The app catalog uses the same submission metadata source introduced in Phase 18.
    - Phase 20 verification can reuse the catalog selectors and submitted-app data without a second data source.
    - Browseability is derived from metadata, not handwritten UI-only lists.
---

<objective>
Build the metadata-driven catalog source and selectors.

Purpose: Extend the Phase 18 submission foundations into a real catalog data source before building any UI, so the first in-product catalog is driven entirely by checked-in reviewed metadata.
Output: Catalog-ready sample data and reusable catalog selectors with focused tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-app-catalog/19-CONTEXT.md
@src/features/platform/submittedAppManifests.ts
@src/features/platform/submitted-apps/studio-lab.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the catalog-ready submission data source</name>
  <files>src/features/platform/submittedAppManifests.ts, src/features/platform/submitted-apps/signal-box.json, src/features/platform/submittedAppManifests.test.ts</files>
  <action>Add at least one more valid submitted app manifest and ensure the shared submission metadata source surfaces multiple catalog-ready entries for the first in-product catalog browse flow.</action>
  <verify>`bun run submissions:check` and `bun run test -- src/features/platform/submittedAppManifests.test.ts` pass.</verify>
  <done>The future catalog has a real reviewed metadata set to browse.</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable catalog selectors</name>
  <files>src/features/apps/catalog/appCatalogModel.ts, src/features/apps/catalog/appCatalogModel.test.ts</files>
  <action>Add a small catalog selector layer for categories and filtered entry lists. Keep it deterministic and scoped to the current browse/detail needs.</action>
  <verify>`bun run test -- src/features/apps/catalog/appCatalogModel.test.ts` passes.</verify>
  <done>The catalog UI can consume repo-owned browse selectors instead of hardcoded lists.</done>
</task>

<task type="auto">
  <name>Task 3: Lock category-browse behavior with focused tests</name>
  <files>src/features/apps/catalog/appCatalogModel.test.ts, src/features/platform/submittedAppManifests.test.ts</files>
  <action>Add focused unit coverage for category ordering, filter behavior, and the multi-entry catalog-ready data source. Keep the tests centered on behavior instead of implementation details.</action>
  <verify>`bun run test`, `bun x tsc --noEmit`, and `bun run build` pass after the data-layer changes.</verify>
  <done>The metadata-driven catalog source is ready for the first app surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run submissions:check` passes
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The catalog source is driven entirely by shared reviewed submission metadata
- Category browse behavior is testable before UI work begins
</success_criteria>

<output>
After completion, create `.planning/phases/19-app-catalog/19-01-SUMMARY.md`
</output>
