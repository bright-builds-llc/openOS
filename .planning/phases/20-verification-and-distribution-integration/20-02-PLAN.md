---
phase: 20-verification-and-distribution-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - tests/e2e/distribution-integration.spec.ts
  - tests/e2e/app-integration.spec.ts
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-11T19-00-30
generated_at: 2026-04-11T19:00:30.974Z
must_haves:
  truths:
    - One launcher-path spec proves Notes search/organization, Browser direct entry/fallback, and catalog browsing together.
    - The integrated proof still uses the real app shell, launcher path, and existing submission metadata.
    - The final verification suite adds clarity rather than replacing the focused per-feature specs.
  artifacts:
    - A dedicated milestone-level distribution integration Playwright spec exists.
    - The integrated spec covers Notes, Browser, Library/catalog, and the browse-now/install-later truthfulness boundary.
    - Any small stability fixes required by the final integrated spec are committed.
  key_links:
    - The integrated spec reuses the existing launcher helpers and app surfaces.
    - Submission validation expectations are represented alongside the in-product catalog flow rather than as a disconnected test concern.
    - Phase 20 proves the milestone flows together without inventing new product scope.
---

<objective>
Add the final launcher-path milestone integration proof.

Purpose: Prove the milestone-defining Notes, Browser, submission, and catalog flows work together through the real app shell instead of relying only on isolated specs.
Output: Dedicated `v1.2` distribution-integration Playwright scenario plus any minimal stabilization changes required by the run.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-verification-and-distribution-integration/20-CONTEXT.md
@tests/e2e/fixtures/launcher.ts
@tests/e2e/notes.spec.ts
@tests/e2e/browser-app.spec.ts
@tests/e2e/app-catalog.spec.ts
@scripts/validate-submitted-apps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a final distribution-integration browser spec</name>
  <files>tests/e2e/distribution-integration.spec.ts</files>
  <action>Create one milestone-level launcher-path spec that exercises Notes search/folders, Browser direct navigation/fallback, Library catalog browsing, and the browse-now/install-later distribution truthfulness boundary in one coherent flow.</action>
  <verify>`bun run test:e2e -- tests/e2e/distribution-integration.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The milestone has one explicit integrated browser proof beyond the isolated feature specs.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the existing integration suite aligned with the final milestone behavior</name>
  <files>tests/e2e/app-integration.spec.ts</files>
  <action>Make only the small adjustments needed if the final integrated proof reveals stale assumptions in the existing app-integration flow. Keep the suite readable and phase-scoped.</action>
  <verify>The integration/browser suites remain consistent after the final integrated spec lands.</verify>
  <done>The repo’s launcher-path verification surface tells one coherent milestone story.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize only if the final integrated run exposes a real issue</name>
  <files>tests/e2e/distribution-integration.spec.ts, tests/e2e/app-integration.spec.ts</files>
  <action>Make only the smallest changes required if the integrated spec exposes instability in the final milestone flows. Do not broaden scope or add unrelated UX changes.</action>
  <verify>The dedicated integration spec and the full `webkit-iphone` suite both pass.</verify>
  <done>The final integrated proof is stable enough to serve as milestone evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:e2e -- tests/e2e/distribution-integration.spec.ts --project=webkit-iphone` passes
- [ ] `bun run test:e2e --project=webkit-iphone` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The milestone has one explicit launcher-path integration proof
- The final integrated proof remains faithful to the shipped product flows
</success_criteria>

<output>
After completion, create `.planning/phases/20-verification-and-distribution-integration/20-02-SUMMARY.md`
</output>
