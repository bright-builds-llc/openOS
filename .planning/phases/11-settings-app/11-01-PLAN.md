---
phase: 11-settings-app
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/settingsPreferences.ts
  - src/features/settings/settingsPreferences.test.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
  - src/features/shell/theme/shellTheme.css
autonomous: true
requirements:
  - SETT-02
  - SETT-03
user_setup: []
must_haves:
  truths:
    - openOS preferences are defined through a typed shared store rather than ad hoc UI state.
    - At least one or two preferences have visible effect on the running shell.
    - Preference values persist locally across relaunches.
  artifacts:
    - A typed settings preference module exists.
    - Preference store coverage exists.
    - The shell reads effective preferences from the shared settings store.
  key_links:
    - Settings persistence is separate from install/launch helper storage.
    - Preference writes flow into a shared store that later Settings UI can consume.
    - Visible shell behavior proves the preferences are real, not decorative.
---

<objective>
Build the shared openOS preference store and apply it to the shell.

Purpose: Give the Settings app real state to control by introducing typed preference storage and a small set of visibly applied preferences before the UI lands.
Output: Preference schema/store, persistence coverage, and shell integration for visibly applied settings.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-settings-app/11-RESEARCH.md
@src/features/platform/appStorage.ts
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/theme/shellTheme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a typed openOS preference store</name>
  <files>src/features/settings/settingsPreferences.ts, src/features/settings/settingsPreferences.test.ts</files>
  <action>Create a typed preference module for openOS-specific settings with defaults, persistence, and read/write helpers. Keep the surface narrowly scoped to settings needed in this milestone and separate from existing install/launch storage helpers.</action>
  <verify>`pnpm test` passes with focused coverage for preference defaults, persistence, and retrieval.</verify>
  <done>The codebase has a shared settings store ready for the Settings UI to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Apply a small visible preference set to the shell</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/shell/theme/shellTheme.css, src/features/settings/settingsPreferences.ts</files>
  <action>Wire one or two clearly visible openOS preferences into the running shell, such as appearance/wallpaper or motion-related behavior, so the future Settings app controls something real from day one.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and the shell visibly reflects the configured preference values.</verify>
  <done>The preference store is connected to real shell behavior instead of decorative future-only toggles.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the preference application path stable for later app UI</name>
  <files>src/features/settings/settingsPreferences.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Make any small cleanup needed so later Settings UI can read and write the same preference store without reworking the shell integration. Do not build the Settings app UI in this plan.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The shared preference path is ready for the Settings app phase work that follows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- openOS preferences are typed, persistent, and visibly applied
- The shell preference path is ready for the Settings app UI
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-app/11-01-SUMMARY.md`
</output>
