---
phase: 10-app-platform-primitives
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - src/features/runtime/AppSurface.tsx
  - src/features/runtime/ComingSoonApp.tsx
  - src/features/platform/appDefinitions.ts
  - .planning/phases/10-app-platform-primitives/10-03-SUMMARY.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - The new platform primitives are integrated into existing runtime paths rather than remaining dead abstraction.
    - Current shipped app surfaces still work after the platform refactor.
    - The phase leaves the codebase ready for `Settings`, `Notes`, and `Browser` to consume the new platform layer directly.
  artifacts:
    - Existing runtime/app-surface consumers are aligned with the new platform layer where appropriate.
    - A final Phase 10 summary exists documenting how the platform layer is now live in the codebase.
    - Final verification proves no regression to the current launcher/runtime path.
  key_links:
    - Existing runtime consumers read from the platform-backed app definitions instead of bypassing them.
    - Current app surfaces remain compatible with the enriched app metadata.
    - The platform layer demonstrably reduces future ad hoc wiring for the next app phases.
---

<objective>
Integrate the platform primitives into the shipped runtime path and finalize Phase 10 readiness.

Purpose: Ensure the internal app-platform layer is real, integrated, and non-regressive before `Settings`, `Notes`, and `Browser` depend on it.
Output: Final integration adjustments plus Phase 10 verification-ready summary artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-app-platform-primitives/10-RESEARCH.md
@src/features/platform/appDefinitions.ts
@src/features/runtime/AppSurface.tsx
@src/features/runtime/ComingSoonApp.tsx
@src/features/runtime/appRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align existing runtime consumers with the platform-backed app definitions</name>
  <files>src/features/runtime/AppSurface.tsx, src/features/runtime/ComingSoonApp.tsx, src/features/platform/appDefinitions.ts</files>
  <action>Make the smallest useful adjustments needed so current runtime/app-surface consumers are explicitly aligned with the new platform-backed app definitions and metadata. Do not add user-facing Settings UI here; just ensure the platform layer is meaningfully connected to the shipped runtime path.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the integration adjustments.</verify>
  <done>The platform layer is clearly part of the real runtime path instead of future-only scaffolding.</done>
</task>

<task type="auto">
  <name>Task 2: Run the final verification set for the platform-primitives phase</name>
  <files>src/features/platform/appDefinitions.ts, src/features/runtime/appRegistry.ts</files>
  <action>Run the repo’s baseline unit, type, and build checks against the new platform layer and fix only if a failure is clearly within the phase’s internal-platform scope.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The internal platform layer is stable enough for the next app phases to build on.</done>
</task>

<task type="auto">
  <name>Task 3: Record the integration outcome in the plan summary</name>
  <files>.planning/phases/10-app-platform-primitives/10-03-SUMMARY.md</files>
  <action>Write the final plan summary so it clearly explains how the platform primitives are now live in the codebase and what they make easier for `Settings`, `Notes`, and `Browser`.</action>
  <verify>The summary accurately reflects the integrated platform surface and the passed verification results.</verify>
  <done>The phase closes with explicit evidence that the platform layer is useful and non-speculative.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The new platform primitives are integrated into the shipped runtime path
- The codebase is ready for the Settings, Notes, and Browser phases to consume the platform layer directly
</success_criteria>

<output>
After completion, create `.planning/phases/10-app-platform-primitives/10-03-SUMMARY.md`
</output>
