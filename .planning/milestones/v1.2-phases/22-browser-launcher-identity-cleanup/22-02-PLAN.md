---
phase: 22-browser-launcher-identity-cleanup
plan: "02"
type: execute
wave: 1
depends_on:
  - "22-01"
files_modified:
  - tests/e2e/browser-app.spec.ts
  - tests/e2e/settings.spec.ts
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-04-12T01-21-17
generated_at: 2026-04-12T01:21:17.070Z
must_haves:
  truths:
    - Browser launcher-path verification explicitly exercises both the page-grid and dock entrypoints.
    - Existing Browser direct-entry and truthful-fallback behavior remain intact after the identity cleanup.
    - Settings and runtime-visible Browser metadata stay coherent with the shared Browser namespace.
  artifacts:
    - The Browser Playwright coverage includes a dock-entry assertion and shared-namespace assertion.
    - Settings launcher-path verification asserts the canonical Browser row exposes the shared namespace.
    - The final repo-native verification path still passes with the Browser cleanup in place.
  key_links:
    - Dock and grid launcher icons both resolve to the same Browser runtime surface.
    - The canonical Browser settings row reflects the shared Browser metadata instead of divergent launcher-specific storage.
    - The milestone verification command continues to protect Browser launcher-path behavior end to end.
---

<objective>
Close the remaining Browser launcher-path coverage gap while proving the metadata cleanup is visible in shipped product surfaces.

Purpose: Add the missing dock-entry Browser proof, keep the direct-navigation behavior intact, and show that Settings/runtime metadata now present one coherent Browser identity.
Output: Updated Playwright coverage for Browser and Settings plus final verification evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v1.2-v1.2-MILESTONE-AUDIT.md
@.planning/phases/17-browser-direct-navigation/17-VERIFICATION.md
@.planning/phases/22-browser-launcher-identity-cleanup/22-CONTEXT.md
@tests/e2e/browser-app.spec.ts
@tests/e2e/settings.spec.ts
@tests/e2e/fixtures/launcher.ts
@src/features/apps/settings/SettingsApp.tsx
@src/features/runtime/AppSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Browser launcher-path coverage to the dock entrypoint</name>
  <files>tests/e2e/browser-app.spec.ts</files>
  <read_first>
    - tests/e2e/browser-app.spec.ts
    - tests/e2e/fixtures/launcher.ts
    - src/features/shell/AdaptiveShellFoundation.tsx
    - .planning/phases/17-browser-direct-navigation/17-VERIFICATION.md
  </read_first>
  <action>Update the Browser Playwright spec so it explicitly proves both Browser launcher entrypoints can open the same Browser runtime surface. Keep one focused flow that still covers direct inline navigation and truthful fallback, and add a lightweight dock-entry proof that asserts the shared Browser storage namespace is surfaced on the runtime app shell.</action>
  <acceptance_criteria>
    - `tests/e2e/browser-app.spec.ts` explicitly opens Browser from both `browser-grid` and `browser`
    - The Browser spec still proves direct local navigation and external fallback behavior
    - The Browser runtime surface shows the shared Browser storage namespace regardless of entrypoint
  </acceptance_criteria>
  <verify>`bun run test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The missing dock-entry Browser proof is now part of the shipped launcher-path verification surface.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Settings metadata coherent with the shared Browser identity</name>
  <files>tests/e2e/settings.spec.ts</files>
  <read_first>
    - tests/e2e/settings.spec.ts
    - src/features/apps/settings/SettingsApp.tsx
    - src/features/runtime/appRegistry.ts
  </read_first>
  <action>Refine the Settings Playwright assertions so the canonical Browser management row still appears exactly once and now shows the shared Browser storage namespace. Keep the test centered on user-visible metadata truthfulness instead of internal implementation details.</action>
  <acceptance_criteria>
    - `tests/e2e/settings.spec.ts` still proves there is one Browser management row
    - The Settings Browser row now asserts the shared Browser namespace
    - No duplicate Browser row appears in Settings
  </acceptance_criteria>
  <verify>`bun run test:e2e -- tests/e2e/settings.spec.ts --project=webkit-iphone` passes.</verify>
  <done>Settings exposes one coherent Browser management identity after the cleanup.</done>
</task>

</tasks>

<threat_model>
Primary risk: the Browser identity cleanup could look correct in unit metadata tests while the real launcher paths or Settings surface still behave inconsistently.
Mitigation required: prove both Browser entrypoints and the Settings Browser row in Playwright, then rerun the repo-native verification command.
Block on: any path where dock launch, direct-entry behavior, or metadata surfaces diverge from the shared Browser identity.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bun run test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes
- [ ] `bun run test:e2e -- tests/e2e/settings.spec.ts --project=webkit-iphone` passes
- [ ] `bun run verify:v1.2` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Both Browser launcher entrypoints are explicitly covered by launcher-path verification
- Settings and runtime metadata surfaces remain coherent after the Browser identity cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/22-browser-launcher-identity-cleanup/22-02-SUMMARY.md`
</output>
