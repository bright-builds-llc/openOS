---
phase: 17-browser-direct-navigation
plan: "03"
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - tests/e2e/browser-app.spec.ts
  - src/features/apps/browser/BrowserApp.tsx
  - public/browser-fixtures/direct-url.html
autonomous: true
requirements:
  - BROW-05
  - BROW-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-04-11T18-37-19
generated_at: 2026-04-11T18:37:19.390Z
must_haves:
  truths:
    - Browser-path verification proves direct URL entry beyond the original shortcut list.
    - Browser-path verification proves honest fallback for an arbitrary external URL entered through the address bar.
    - The existing shortcut flow remains stable after direct-navigation work lands.
  artifacts:
    - The Browser Playwright spec covers both direct embedded navigation and direct external fallback.
    - A deterministic local fixture exists for non-curated direct inline navigation.
    - Final repo verification passes with the direct-navigation change set.
  key_links:
    - Browser verification still runs through the real launcher/runtime path.
    - Direct embedded and direct fallback behavior both ride the same Browser app surface.
    - Phase 17 finishes with proof strong enough for later milestone integration verification.
---

<objective>
Verify direct Browser navigation through the real launcher path.

Purpose: Lock the expanded Browser flow behind browser-path proof so direct URL entry, non-curated navigation, and truthful fallback are trusted before later platform phases build on them.
Output: Upgraded Browser Playwright coverage plus final Browser stabilization from the verification run.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-browser-direct-navigation/17-CONTEXT.md
@src/features/apps/browser/BrowserApp.tsx
@src/features/apps/browser/browserDestinations.ts
@tests/e2e/browser-app.spec.ts
@tests/e2e/fixtures/launcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Browser launcher-path verification for direct URLs</name>
  <files>tests/e2e/browser-app.spec.ts, public/browser-fixtures/direct-url.html</files>
  <action>Extend the Browser Playwright scenario so it proves a non-curated same-origin direct URL can render inline and a directly entered arbitrary external URL takes the honest Safari fallback path.</action>
  <verify>`bun run test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The Browser direct-navigation flow is proven through the real launcher/runtime path.</done>
</task>

<task type="auto">
  <name>Task 2: Make only the small Browser fixes needed for browser-path stability</name>
  <files>src/features/apps/browser/BrowserApp.tsx, tests/e2e/browser-app.spec.ts</files>
  <action>Make only the small Browser-specific adjustments required if the expanded browser scenario exposes instability in direct entry, inline rendering, or fallback behavior. Do not broaden into tabs or Safari-parity scope.</action>
  <verify>The focused Browser browser scenario passes consistently and `bun run build` stays green.</verify>
  <done>The covered Browser path feels stable and representative of the intended milestone behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final Browser verification set</name>
  <files>tests/e2e/browser-app.spec.ts, src/features/apps/browser/BrowserApp.tsx</files>
  <action>Run the focused Browser browser scenario together with the repo’s baseline checks and make only the small stabilization fixes needed if behavior is clearly within Phase 17 scope.</action>
  <verify>`bun run test`, `bun run test:e2e --project=webkit-iphone`, `bun x tsc --noEmit`, and `bun run build` all pass.</verify>
  <done>Browser direct navigation is ready for the remaining milestone phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted Browser Playwright scenario passes
- [ ] `bun run test` passes
- [ ] `bun run test:e2e --project=webkit-iphone` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser direct entry and truthful fallback are browser-verified
- The expanded Browser remains consistent with openOS truthfulness
</success_criteria>

<output>
After completion, create `.planning/phases/17-browser-direct-navigation/17-03-SUMMARY.md`
</output>
