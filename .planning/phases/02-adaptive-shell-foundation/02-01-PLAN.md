---
phase: 02-adaptive-shell-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/install/standalone/StandaloneEntry.tsx
  - src/features/shell/AdaptiveShellFoundation.tsx
  - src/features/shell/components/AmbientBackground.tsx
  - src/features/shell/components/Dock.tsx
  - src/features/shell/components/HomeScreenGrid.tsx
  - src/features/shell/components/StatusBar.tsx
  - src/features/shell/profile/createShellProfile.ts
  - src/features/shell/profile/createShellProfile.test.ts
  - src/features/shell/profile/useShellViewport.ts
  - src/features/shell/shellFoundation.css
autonomous: true
requirements:
  - INST-03
user_setup: []
must_haves:
  truths:
    - The standalone entry path renders a dedicated shell-foundation scene instead of a generic placeholder card.
    - Shell layout values are derived from a pure profile model that accepts viewport and safe-area inputs.
    - Later shell subcomponents can be implemented independently without rewriting the standalone entry flow.
  artifacts:
    - A pure shell-profile module exists with unit coverage.
    - An `AdaptiveShellFoundation` scene exists on the standalone path.
    - Placeholder shell subcomponents exist for status bar, grid, dock, and ambient background.
  key_links:
    - `StandaloneEntry` mounts `AdaptiveShellFoundation`.
    - `AdaptiveShellFoundation` consumes the shell-profile output from the measurement hook.
    - Profile tests verify profile selection and token derivation.
---

<objective>
Establish the adaptive shell foundation and profile system for Phase 2.

Purpose: Give the shell a testable geometry model and stable component boundaries before composition and wallpaper polish begin.
Output: Shell profile core, measurement hook, standalone shell scene, and placeholder shell subcomponents.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-adaptive-shell-foundation/02-CONTEXT.md
@.planning/phases/02-adaptive-shell-foundation/02-RESEARCH.md
@src/features/install/standalone/StandaloneEntry.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a pure shell-profile model</name>
  <files>src/features/shell/profile/createShellProfile.ts, src/features/shell/profile/createShellProfile.test.ts</files>
  <action>Implement a pure profile function that accepts viewport dimensions, aspect ratio, and safe-area insets, then produces a small set of profile kinds plus shell tokens for icon size, spacing, dock offset, and top-chrome spacing. Keep the model hybrid: discrete profile kinds with gentle interpolation-ready values, not a table of hard-coded devices.</action>
  <verify>`pnpm test -- --run src/features/shell/profile/createShellProfile.test.ts` passes.</verify>
  <done>Profile derivation is explicit, testable, and matches the locked adaptation rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add viewport measurement and shell scene boundaries</name>
  <files>src/features/shell/profile/useShellViewport.ts, src/features/shell/AdaptiveShellFoundation.tsx, src/features/install/standalone/StandaloneEntry.tsx, src/features/shell/shellFoundation.css</files>
  <action>Create a thin measurement hook that reads viewport dimensions, safe-area fallbacks, and reduced-motion state where relevant, then feeds the pure profile model. Mount a new `AdaptiveShellFoundation` scene from `StandaloneEntry`, and publish shell tokens as CSS variables at the scene root.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The standalone path now renders through a dedicated shell scene driven by profile tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder shell component seams for Wave 2</name>
  <files>src/features/shell/components/StatusBar.tsx, src/features/shell/components/HomeScreenGrid.tsx, src/features/shell/components/Dock.tsx, src/features/shell/components/AmbientBackground.tsx, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Create real but intentionally shallow components for status bar, home grid, dock, and ambient background. Wire them into the shell scene now so the composition plan and the ambient-theme plan can proceed in parallel with disjoint file ownership.</action>
  <verify>`pnpm build` passes with the shell scene rendering through the new subcomponents.</verify>
  <done>Wave-2 plans can modify separate component files without restructuring the scene tree.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/shell/profile/createShellProfile.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell foundation uses a profile model instead of single-device constants
- Wave-2 composition and wallpaper work have clean component seams
</success_criteria>

<output>
After completion, create `.planning/phases/02-adaptive-shell-foundation/02-01-SUMMARY.md`
</output>
