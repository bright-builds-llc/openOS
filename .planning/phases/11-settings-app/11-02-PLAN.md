---
phase: 11-settings-app
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/features/apps/settings/SettingsApp.tsx
  - src/features/apps/settings/settings.css
  - src/features/runtime/appRegistry.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
user_setup: []
must_haves:
  truths:
    - `Settings` is now a real implemented app in the shared launcher/runtime path.
    - Users can view and change the available openOS preferences from inside Settings.
    - Settings changes persist locally across relaunches.
  artifacts:
    - A real Settings app module exists.
    - The runtime registry marks Settings as implemented and routes it through its own app surface.
    - Settings UI styling exists and feels like an openOS-native control plane.
  key_links:
    - Settings UI reads and writes the shared preference store from Plan 01.
    - The runtime opens Settings through the same shared app surface and motion path as other implemented apps.
    - Visible preference changes prove the Settings controls are functionally connected.
---

<objective>
Implement the Settings app UI on top of the shared preference store.

Purpose: Deliver Settings as a real implemented app that controls the shared openOS preference store through a believable openOS-native app surface.
Output: Settings app module, implemented-app routing, and persistent preference controls.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-settings-app/11-RESEARCH.md
@src/features/settings/settingsPreferences.ts
@src/features/runtime/appRegistry.ts
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/runtime/AppSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the real Settings app module and route it through the runtime</name>
  <files>src/features/apps/settings/SettingsApp.tsx, src/features/runtime/appRegistry.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Create the `Settings` app component and route it through the shared runtime as an implemented app. Keep the app surface native to openOS and avoid overbuilding system-settings breadth in this first version.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and Settings opens through the normal launcher path.</verify>
  <done>Settings is a real implemented app instead of a coming-soon placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Build grouped Settings UI for the available preferences</name>
  <files>src/features/apps/settings/SettingsApp.tsx, src/features/apps/settings/settings.css</files>
  <action>Implement grouped preference controls for the currently supported openOS settings. The UI should feel like a control plane for openOS itself, not a generic form page.</action>
  <verify>Local interaction confirms preference controls are usable and visibly affect the shell.</verify>
  <done>Users can change the available openOS preferences through a coherent Settings UI.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure Settings writes persist and are reloaded correctly</name>
  <files>src/features/apps/settings/SettingsApp.tsx, src/features/settings/settingsPreferences.ts</files>
  <action>Make any small integration fixes needed so Settings writes persist locally and are correctly reloaded on later launches. Keep this narrowly scoped to the current openOS preference set.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass after the Settings integration.</verify>
  <done>The Settings app delivers real persistent preference management rather than transient UI state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell interaction confirms Settings changes persist
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings is a real implemented app with persistent openOS preferences
- The app feels like an openOS-native control plane
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-app/11-02-SUMMARY.md`
</output>
