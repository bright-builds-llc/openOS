---
phase: 23-state-contracts-and-verification-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/appStorage.ts
  - src/features/platform/appStorage.test.ts
  - src/features/platform/appSessionStorage.ts
  - src/features/platform/appSessionStorage.test.ts
  - src/features/runtime/appRegistry.test.ts
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-05-27T21-23-33
generated_at: 2026-05-27T21:40:27Z
must_haves:
  truths:
    - "Session keys for Notes, Browser, Library, and Calculator use canonical app namespaces plus `.session`."
    - "Browser grid and dock launch entries resolve to the same `openos.apps.browser.session` key."
    - "Malformed, unsupported-version, and invalid-payload session snapshots reset to a caller-provided default without throwing."
    - "Blocked or failing storage reads/writes return typed unavailable statuses."
  artifacts:
    - path: "src/features/platform/appStorage.ts"
      provides: "Canonical session-key helper"
      exports:
        - "createAppSessionStorageKey"
    - path: "src/features/platform/appSessionStorage.ts"
      provides: "Typed app-session storage read/write/reset contract"
      exports:
        - "AppSessionStorageLike"
        - "AppSessionReadResult"
        - "AppSessionWriteResult"
        - "AppSessionResetResult"
        - "readAppSessionSnapshot"
        - "writeAppSessionSnapshot"
        - "resetAppSessionSnapshot"
    - path: "src/features/platform/appSessionStorage.test.ts"
      provides: "Focused malformed/reset/unavailable storage contract coverage"
    - path: "src/features/runtime/appRegistry.test.ts"
      provides: "Canonical Browser namespace session-key regression"
  key_links:
    - from: "src/features/platform/appSessionStorage.ts"
      to: "src/features/platform/appStorage.ts"
      via: "createAppSessionStorageKey"
      pattern: "createAppSessionStorageKey"
    - from: "src/features/platform/appSessionStorage.test.ts"
      to: "src/features/platform/appSessionStorage.ts"
      via: "read/write/reset helper imports"
      pattern: "readAppSessionSnapshot"
    - from: "src/features/runtime/appRegistry.test.ts"
      to: "src/features/runtime/appRegistry.ts"
      via: "getCanonicalRuntimeAppStorageNamespace('browser')"
      pattern: "getCanonicalRuntimeAppStorageNamespace"
---

<objective>
Create the shared app-session storage contract for v1.3 without adding a global state manager or visible resume behavior.

Purpose: Phase 23 needs a small platform helper that keeps disposable app-session state separate from durable user data, treats localStorage as untrusted boundary input, and returns typed statuses apps can render later.
Output: Canonical `.session` key helper, typed read/write/reset helpers, and focused Vitest coverage for reset and unavailable behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/features/platform/appStorage.ts
@src/features/platform/appStorage.test.ts
@src/features/platform/appDefinitions.ts
@src/features/runtime/appRegistry.ts
@src/features/runtime/appRegistry.test.ts

<interfaces>
Existing key helpers:
```typescript
export function createAppStorageNamespace(appId: string): string;
export function createAppStorageKey(namespace: string, key: string): string;
export function getAppStorageNamespace(appId: string, maybeApps: RuntimeAppDefinition[]): string | null;
```

Existing canonical runtime selector:
```typescript
export function getCanonicalRuntimeAppStorageNamespace(
  launchSurface: RuntimeAppLaunchSurface,
  maybeApps?: RuntimeApp[],
): string | null;
```

New public contract to create:
```typescript
export type AppSessionStorageLike = Pick<Storage, "getItem" | "removeItem" | "setItem">;

export type AppSessionResetReason =
  | "malformed-json"
  | "unsupported-version"
  | "invalid-payload";

export type AppSessionReadResult<TSession> =
  | { status: "missing"; session: TSession }
  | { status: "loaded"; session: TSession }
  | { status: "reset"; session: TSession; reason: AppSessionResetReason }
  | {
      status: "unavailable";
      session: TSession;
      reason: "storage-read-failed" | "storage-reset-failed";
      error: unknown;
    };

export type AppSessionWriteResult =
  | { status: "saved" }
  | {
      status: "unavailable";
      reason: "storage-write-failed";
      error: unknown;
    };

export type AppSessionResetResult =
  | { status: "reset" }
  | {
      status: "unavailable";
      reason: "storage-reset-failed";
      error: unknown;
    };

export type AppSessionPayloadParser<TSession> = (
  maybeSession: unknown,
) => TSession | null;
```
</interfaces>

Locked decisions implemented here: D-01, D-04, D-05, D-06, D-08, D-09, D-12. Do not implement deferred Notes v3 migration, Browser tabs, submitted registry generation, or Library/Calculator visible resume behavior.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical session-key helper and namespace regressions</name>
  <files>src/features/platform/appStorage.ts, src/features/platform/appStorage.test.ts, src/features/runtime/appRegistry.test.ts</files>
  <read_first>
    - .planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
    - .planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
    - src/features/platform/appStorage.ts
    - src/features/platform/appStorage.test.ts
    - src/features/platform/appDefinitions.ts
    - src/features/runtime/appRegistry.ts
    - src/features/runtime/appRegistry.test.ts
  </read_first>
  <behavior>
    - `createAppSessionStorageKey("openos.apps.notes")` returns exactly `openos.apps.notes.session`.
    - Notes, Browser, Library, and Calculator session keys are exactly `openos.apps.notes.session`, `openos.apps.browser.session`, `openos.apps.library.session`, and `openos.apps.calculator.session`.
    - Browser grid and dock entries both produce the same `openos.apps.browser.session` key.
    - `getCanonicalRuntimeAppStorageNamespace("browser")` still resolves to `openos.apps.browser` before the session suffix is applied.
  </behavior>
  <action>Add and export `createAppSessionStorageKey(namespace: string): string` from `src/features/platform/appStorage.ts`; it must return `createAppStorageKey(namespace, "session")` and must not replace or rename existing namespace/key helpers. Update `appStorage.test.ts` to import the new helper and assert the exact session-key strings for Notes, Browser, Library, and Calculator using `builtInAppDefinitions` storage namespaces per D-01. Add a regression in `appStorage.test.ts` proving the `browser-grid` and `browser` definitions both produce `openos.apps.browser.session` per D-12. Update `appRegistry.test.ts` to import `createAppSessionStorageKey` and assert `createAppSessionStorageKey(getCanonicalRuntimeAppStorageNamespace("browser") ?? "")` is exactly `openos.apps.browser.session`. Do not modify `HomeScreenRuntimeState`, app components, or any durable Notes storage code in this task per D-07 and D-09.</action>
  <acceptance_criteria>
    - `src/features/platform/appStorage.ts` exports a function named `createAppSessionStorageKey`.
    - `src/features/platform/appStorage.ts` contains the exact expression `createAppStorageKey(namespace, "session")`.
    - `src/features/platform/appStorage.test.ts` contains exact expected strings `openos.apps.notes.session`, `openos.apps.browser.session`, `openos.apps.library.session`, and `openos.apps.calculator.session`.
    - `src/features/platform/appStorage.test.ts` has a Browser grid/dock assertion where both entries resolve to `openos.apps.browser.session`.
    - `src/features/runtime/appRegistry.test.ts` contains an assertion that canonical Browser launch-surface storage becomes `openos.apps.browser.session`.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/features/platform/appStorage.test.ts src/features/runtime/appRegistry.test.ts</automated>
  </verify>
  <done>Canonical session key construction is exported and covered, including the locked Browser canonical namespace behavior from Phase 22.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed app-session read/write/reset helper</name>
  <files>src/features/platform/appSessionStorage.ts, src/features/platform/appSessionStorage.test.ts</files>
  <read_first>
    - .planning/phases/23-state-contracts-and-verification-baseline/23-CONTEXT.md
    - .planning/phases/23-state-contracts-and-verification-baseline/23-RESEARCH.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
    - src/features/platform/appStorage.ts
    - src/features/platform/appStorage.test.ts
  </read_first>
  <behavior>
    - Missing session key returns `{ status: "missing", session: defaultSession }`.
    - Valid stored `{ version, session }` envelope returns `{ status: "loaded", session }`.
    - Malformed JSON returns `{ status: "reset", reason: "malformed-json", session: defaultSession }` after writing the default snapshot to the exact `.session` key.
    - Stored envelope with a nonmatching version returns reset reason `unsupported-version`.
    - Stored envelope whose `session` fails the caller parser returns reset reason `invalid-payload`.
    - `getItem` failures return `{ status: "unavailable", reason: "storage-read-failed", session: defaultSession, error }`.
    - `setItem` failures during explicit writes return `{ status: "unavailable", reason: "storage-write-failed", error }`.
    - Reset removes only the exact `.session` key and returns `{ status: "reset" }`; remove failures return unavailable reason `storage-reset-failed`.
  </behavior>
  <action>Create `src/features/platform/appSessionStorage.ts` with a functional core around JSON/envelope parsing and a thin imperative shell around `StorageLike` calls per D-08. Export `AppSessionStorageLike`, `AppSessionResetReason`, `AppSessionReadResult`, `AppSessionWriteResult`, `AppSessionResetResult`, `AppSessionPayloadParser`, `readAppSessionSnapshot`, `writeAppSessionSnapshot`, and `resetAppSessionSnapshot`. The stored envelope must be exactly `{ version: number; session: TSession }`. `readAppSessionSnapshot(storage, namespace, { version, defaultSession, parseSession })` must compute the key with `createAppSessionStorageKey(namespace)`, treat localStorage content as untrusted, catch JSON and storage exceptions, and return only the statuses listed in this task. On malformed JSON, unsupported version, or invalid payload, it must write `JSON.stringify({ version, session: defaultSession })` to the same session key; if that reset write throws, return `status: "unavailable"` with reason `storage-reset-failed`. `writeAppSessionSnapshot(storage, namespace, { version, session })` must return `saved` only after `storage.setItem` succeeds; if set throws, return `unavailable` with reason `storage-write-failed` per D-06. `resetAppSessionSnapshot(storage, namespace)` must call `storage.removeItem(createAppSessionStorageKey(namespace))` only and must not call `clear()` or touch any durable keys per D-02 and D-03. Add `appSessionStorage.test.ts` with focused Vitest tests using Arrange/Act/Assert comments and throwing storage doubles. Do not import Notes, Browser, Library, Calculator payload types, React, or any new runtime dependency per D-04 and D-09.</action>
  <acceptance_criteria>
    - `src/features/platform/appSessionStorage.ts` exists and exports all nine names listed in the action.
    - `src/features/platform/appSessionStorage.ts` contains no imports from `src/features/apps/` and no React imports.
    - `src/features/platform/appSessionStorage.test.ts` has tests whose names include `missing`, `loaded`, `malformed`, `unsupported`, `invalid`, `read failure`, `write failure`, and `exact session key`.
    - The tests assert exact statuses: `missing`, `loaded`, `reset`, `unavailable`, and `saved`.
    - The tests assert exact reasons: `malformed-json`, `unsupported-version`, `invalid-payload`, `storage-read-failed`, `storage-write-failed`, and `storage-reset-failed`.
    - The exact-key reset test proves a neighboring durable key such as `openos.apps.notes.notes` remains present after resetting `openos.apps.notes.session`.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/features/platform/appSessionStorage.test.ts</automated>
  </verify>
  <done>The app-session helper provides typed missing/loaded/reset/unavailable read results, saved/unavailable write results, exact-key reset behavior, and no app-owned payload schemas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> appSessionStorage | Stored strings are user/browser-controlled and can be malformed, tampered, missing, or inaccessible. |
| appSessionStorage -> future app shells | Future apps will render status results; the helper must not lie about persistence success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering / Denial of Service | `readAppSessionSnapshot` | mitigate | Catch `JSON.parse` failures, validate envelope version and app parser result, return `reset` with the caller default session instead of throwing. |
| T-23-02 | Denial of Service | `readAppSessionSnapshot`, `writeAppSessionSnapshot`, `resetAppSessionSnapshot` | mitigate | Catch `getItem`, `setItem`, and `removeItem` exceptions and return typed `unavailable` statuses with exact reasons. |
| T-23-03 | Tampering / Information Loss | `resetAppSessionSnapshot` | mitigate | Reset only `createAppSessionStorageKey(namespace)` and add tests proving neighboring durable keys remain untouched. |
| T-23-04 | Information Disclosure | app-session snapshots | accept | Phase 23 stores disposable UI context only, no auth tokens or secrets; future payloads remain app-owned and must keep secrets out of local session snapshots. |
</threat_model>

<verification>
Run the focused contract checks:
`bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appSessionStorage.test.ts src/features/runtime/appRegistry.test.ts`

Run type coverage for the new discriminated unions:
`bun x tsc --noEmit`
</verification>

<success_criteria>
- `createAppSessionStorageKey` returns canonical `.session` keys for app namespaces.
- Browser grid/dock storage identity stays locked to `openos.apps.browser.session`.
- Session reads return only `missing`, `loaded`, `reset`, or `unavailable`.
- Session writes return only `saved` or `unavailable`.
- Reset behavior touches only the exact `.session` key.
- No new runtime dependency or global state manager is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/23-state-contracts-and-verification-baseline/23-01-SUMMARY.md`.
</output>
