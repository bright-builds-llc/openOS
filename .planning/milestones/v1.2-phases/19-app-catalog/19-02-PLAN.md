---
phase: 19-app-catalog
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/features/apps/catalog/AppCatalogApp.tsx
  - src/features/apps/catalog/appCatalog.css
  - src/features/platform/appDefinitions.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - PLAT-05
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 19-2026-04-11T19-00-30
generated_at: 2026-04-11T19:00:30.974Z
must_haves:
  truths:
    - Users can open and browse an in-product app catalog through a real built-in app surface.
    - Catalog entries are rendered from the shared submitted-app metadata source rather than a handwritten UI list.
    - The catalog stays explicit that installation comes later.
  artifacts:
    - `Library` is promoted from coming-soon to the real catalog surface.
    - The catalog app includes category browse and entry-detail inspection.
    - Catalog styling stays coherent with the existing openOS app shell.
  key_links:
    - The catalog surface consumes the Phase 19 selector layer and the Phase 18 submission metadata source.
    - The `Library` launcher icon now routes to the real catalog app through the shared runtime path.
    - The product copy makes the browse-now/install-later boundary explicit.
---

<objective>
Implement the first in-product app catalog surface.

Purpose: Turn the reviewed submission metadata into a real built-in catalog browse/detail experience without overclaiming install support.
Output: Catalog app surface, runtime routing, and coherent built-in app styling.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-app-catalog/19-CONTEXT.md
@src/features/platform/submittedAppManifests.ts
@src/features/apps/catalog/appCatalogModel.ts
@src/features/platform/appDefinitions.ts
@src/features/shell/AdaptiveShellFoundation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Library through the real catalog app surface</name>
  <files>src/features/platform/appDefinitions.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Promote `Library` from a placeholder to the first real catalog surface using the shared runtime/app-surface path. Keep it as the existing launcher icon rather than adding a new app entry.</action>
  <verify>The Library icon opens a real catalog app through the launcher path.</verify>
  <done>The first in-product app catalog is reachable as part of the shipped shell.</done>
</task>

<task type="auto">
  <name>Task 2: Implement metadata-driven browse and detail UI</name>
  <files>src/features/apps/catalog/AppCatalogApp.tsx, src/features/apps/catalog/appCatalog.css</files>
  <action>Implement a browse/detail catalog surface with category chips, entry list, and detail panel driven by the reviewed submission metadata source. Keep the copy explicit that install support comes later.</action>
  <verify>Local interaction confirms category switching, entry selection, and detail inspection work inside the catalog app.</verify>
  <done>Users can browse and inspect meaningful app information in-product.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the catalog visually coherent with the built-in app shell</name>
  <files>src/features/apps/catalog/appCatalog.css</files>
  <action>Give the catalog a strong built-in-app feel with a clear hero, calm browse hierarchy, and restrained detail layout while preserving the existing openOS UI language.</action>
  <verify>`bun x tsc --noEmit` and `bun run build` pass after the catalog UI changes.</verify>
  <done>The catalog feels like an intentional built-in openOS surface instead of a generic marketplace card grid.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
- [ ] Local interaction confirms category browse and detail inspection work through the Library app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can browse and inspect a metadata-driven in-product app catalog
- The catalog stays explicit that browsing now does not mean installation now
</success_criteria>

<output>
After completion, create `.planning/phases/19-app-catalog/19-02-SUMMARY.md`
</output>
