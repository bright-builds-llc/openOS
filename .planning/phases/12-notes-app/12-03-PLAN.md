---
phase: 12-notes-app
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified:
  - tests/e2e/notes.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/apps/notes/NotesApp.tsx
  - .planning/phases/12-notes-app/12-03-SUMMARY.md
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-04
user_setup: []
must_haves:
  truths:
    - Browser-level verification proves Notes can be opened, a note can be created/edited/reopened, and the local-only warning is visible.
    - Notes persistence is browser-verified through the real launcher/runtime path, not component-only tests.
    - The phase leaves the codebase ready for Browser and milestone-level verification to consume the same platform foundations.
  artifacts:
    - A focused Notes Playwright spec exists.
    - Notes local-only warning is part of the browser-verifiable UI surface.
    - Any final integration polish required to stabilize Notes behavior is committed.
  key_links:
    - Notes browser verification uses the real launcher/runtime path.
    - The local-only/no-sync messaging is asserted through the shipped app UI.
    - Reopen behavior is proven through the same storage/repository path used by the Notes app.
---

<objective>
Verify Notes end to end and lock the local-only notes experience.

Purpose: Add focused browser verification for the Notes flow and make any small stabilization fixes needed so local persistence and no-sync messaging are trustworthy.
Output: Notes browser verification plus final Notes stabilization inside the shared shell/runtime path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-notes-app/12-RESEARCH.md
@src/features/apps/notes/NotesApp.tsx
@src/features/apps/notes/notesStorage.ts
@tests/e2e/fixtures/launcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused Notes browser verification</name>
  <files>tests/e2e/notes.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a Playwright scenario that launches Notes through the real shell path, creates a note, edits it, reloads or relaunches, and confirms the note is still available. Also assert that the local-only/no-sync warning is visible.</action>
  <verify>`pnpm test:e2e -- tests/e2e/notes.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The Notes local-only flow is browser-verified instead of relying only on unit tests or local manual checks.</done>
</task>

<task type="auto">
  <name>Task 2: Make only the small Notes fixes required for stable behavior</name>
  <files>src/features/apps/notes/NotesApp.tsx, tests/e2e/notes.spec.ts</files>
  <action>Make only the minimal Notes-specific adjustments required if the browser scenario exposes instability in create/edit/reopen behavior or local-only messaging visibility. Do not broaden into search, sync, or rich text scope.</action>
  <verify>The focused Notes browser scenario passes consistently and `pnpm build` remains green.</verify>
  <done>The covered Notes path feels stable and representative of the intended milestone behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final verification set for the Notes phase</name>
  <files>tests/e2e/notes.spec.ts, src/features/apps/notes/NotesApp.tsx</files>
  <action>Run the focused Notes browser scenario together with the repo’s baseline checks and make only the small stabilization fixes needed if behavior is clearly within the Notes phase scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The Notes app is ready as the second real built-in app for `v1.1`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted Notes Playwright scenario passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Notes is browser-verified for create/edit/reopen behavior and local-only messaging
- The local-only notes experience is stable enough for the remaining milestone phases
</success_criteria>

<output>
After completion, create `.planning/phases/12-notes-app/12-03-SUMMARY.md`
</output>
