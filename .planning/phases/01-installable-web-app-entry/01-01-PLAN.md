---
phase: 01-installable-web-app-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/app/AppRoot.tsx
  - src/app/bootstrap/createAppContext.ts
  - src/lib/platform/detectInstallContext.ts
  - src/lib/platform/detectInstallContext.test.ts
  - src/features/install/browser/BrowserInstallFlow.tsx
  - src/features/install/standalone/StandaloneEntry.tsx
  - src/styles/globals.css
autonomous: true
requirements:
  - INST-01
  - INST-02
user_setup: []
must_haves:
  truths:
    - The repository can run and build a React + TypeScript + Vite client app.
    - Runtime entry mode is derived from real browser signals rather than hard-coded assumptions.
    - The root app can branch between browser-mode onboarding and standalone entry surfaces.
    - Developers can deterministically exercise the standalone branch locally without needing a real installed web app for every verification pass.
  artifacts:
    - Frontend scaffold files exist for Vite, React, and TypeScript.
    - A typed install-context detection module exists with unit coverage.
    - Browser and standalone entry components exist as separate extension points for later plans.
    - A dev-only install-context override exists for local verification.
  key_links:
    - App bootstrap uses the install-context detection module to choose the entry surface.
    - Unit tests cover the pure install-context logic used by the root app.
    - The local override path flows through the same normalized install-context model as the real runtime path.
---

<objective>
Establish the technical foundation for Phase 1 without overbuilding later shell work.

Purpose: Give the phase a real frontend app, explicit install-context detection, and stable extension points for browser-mode onboarding and standalone-mode entry.
Output: Vite/React/TypeScript scaffold, root app wiring, typed install-context logic, and browser/standalone component stubs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-installable-web-app-entry/01-CONTEXT.md
@.planning/phases/01-installable-web-app-entry/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the client app and root entry wiring</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/app/AppRoot.tsx, src/styles/globals.css</files>
  <action>Create a minimal Vite + React + TypeScript application scaffold using pnpm conventions. Wire a root `AppRoot` that does only one job: choose between browser-mode onboarding and standalone-mode entry surfaces. Keep the visual shell intentionally light in this plan so Phase 2 still owns responsive shell fidelity.</action>
  <verify>`pnpm install` then `pnpm build` completes successfully.</verify>
  <done>The repo has a runnable frontend app, the root app compiles, and the top-level branch between browser and standalone entry surfaces is explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add install-context detection as a typed functional core</name>
  <files>src/app/bootstrap/createAppContext.ts, src/lib/platform/detectInstallContext.ts, src/lib/platform/detectInstallContext.test.ts</files>
  <action>Implement install-context detection as pure logic that normalizes `display-mode` and legacy iOS standalone signals into a small typed model. Add a clearly non-production local override path so later plans can verify standalone behavior deterministically without needing a real installed app on every run. Keep browser API touching code thin and keep the branching logic unit-testable to match the repo standards.</action>
  <verify>`pnpm test -- --run detectInstallContext` or the equivalent targeted Vitest command passes.</verify>
  <done>The install-context logic is isolated, typed, covered by unit tests, and supports deterministic local verification of browser vs standalone behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create stable extension points for browser and standalone entry work</name>
  <files>src/features/install/browser/BrowserInstallFlow.tsx, src/features/install/standalone/StandaloneEntry.tsx, src/app/AppRoot.tsx</files>
  <action>Create stubbed but real component boundaries for the two wave-2 plans. The goal is to let browser-mode onboarding and standalone identity/launch work proceed without both plans needing to keep rewriting the root app. Keep the stubs functional, compile-safe, and intentionally shallow.</action>
  <verify>`pnpm build` still passes after the stubs are wired.</verify>
  <done>Wave-2 plans can work in separate files without needing to redesign the app root.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` completes
- [ ] `pnpm test -- --run` passes for the new pure logic tests
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root app branching is explicit and testable
- Phase 1 has a stable code foundation without pulling Phase 2 shell work forward
</success_criteria>

<output>
After completion, create `.planning/phases/01-installable-web-app-entry/01-01-SUMMARY.md`
</output>
