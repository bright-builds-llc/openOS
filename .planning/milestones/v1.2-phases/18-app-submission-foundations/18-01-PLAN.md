---
phase: 18-app-submission-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/submittedAppManifests.ts
  - src/features/platform/submittedAppManifests.test.ts
  - src/features/platform/submitted-apps/studio-lab.json
autonomous: true
requirements:
  - PLAT-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-04-11T18-46-52
generated_at: 2026-04-11T18:46:52.914Z
must_haves:
  truths:
    - Contributors can define submitted apps through a checked-in manifest contract.
    - Submission metadata aligns with the existing app-definition concepts instead of becoming an unrelated schema.
    - Catalog-ready filtering is data-driven and testable before any UI exists.
  artifacts:
    - A submitted app manifest type and validation layer exist.
    - A checked-in sample submission manifest exists.
    - Focused unit coverage exists for the submission contract and validation rules.
  key_links:
    - Submission manifests reuse runtime-aligned icon, settings, and storage metadata concepts.
    - Future catalog work can consume the same submission metadata source directly.
    - Catalog-ready state is derived from validated repo metadata, not manual assumptions.
---

<objective>
Build the repo-owned submission manifest contract.

Purpose: Establish the first checked-in submission metadata source and validator before adding any catalog UI, so contributors and later phases share one stable contract.
Output: Submitted app manifest module, sample manifest, and focused unit coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-app-submission-foundations/18-CONTEXT.md
@src/features/platform/appDefinitions.ts
@src/features/platform/appSettings.ts
@src/features/platform/appStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the submitted app manifest contract</name>
  <files>src/features/platform/submittedAppManifests.ts</files>
  <action>Create a submitted app manifest contract that reuses the current runtime-aligned icon, settings, and storage concepts while staying appropriate for pre-install catalog groundwork.</action>
  <verify>`bun run test -- src/features/platform/submittedAppManifests.test.ts` passes.</verify>
  <done>The repo has a stable metadata contract for submitted apps.</done>
</task>

<task type="auto">
  <name>Task 2: Add a sample checked-in submission manifest</name>
  <files>src/features/platform/submitted-apps/studio-lab.json, src/features/platform/submittedAppManifests.ts</files>
  <action>Add the first sample submitted app manifest and wire it into the shared submission metadata source that future catalog work can consume.</action>
  <verify>The sample manifest validates successfully through the repo-owned validation flow.</verify>
  <done>The submission contract is proven by a real checked-in sample path.</done>
</task>

<task type="auto">
  <name>Task 3: Lock validation behavior with focused unit coverage</name>
  <files>src/features/platform/submittedAppManifests.test.ts</files>
  <action>Add focused unit coverage for valid sample loading, catalog-ready filtering, and invalid review metadata handling. Keep tests centered on behavior instead of implementation details.</action>
  <verify>`bun run test` and `bun x tsc --noEmit` pass after the submission model changes.</verify>
  <done>The repo-driven submission contract is regression-resistant before workflow/docs work lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-driven submission metadata exists and is testable
- Future catalog work can reuse the same metadata source directly
</success_criteria>

<output>
After completion, create `.planning/phases/18-app-submission-foundations/18-01-SUMMARY.md`
</output>
