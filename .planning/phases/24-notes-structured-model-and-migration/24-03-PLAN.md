---
phase: 24-notes-structured-model-and-migration
plan: "03"
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/features/apps/notes/NotesApp.tsx
  - tests/e2e/notes.spec.ts
  - scripts/verify-v1.3.sh
autonomous: true
requirements: [NOTES-01, NOTES-04]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 24-2026-05-28T03-12-00
generated_at: 2026-05-28T03:29:16Z
must_haves:
  truths:
    - "The visible Notes title input and body textarea still work after storage uses structured content."
    - "Browser-level Notes create, search, folder browsing, reload, and persistence behavior remains compatible."
    - "`verify:v1.3` runs focused Notes migration/search/preview tests before the full milestone suite."
  artifacts:
    - path: "src/features/apps/notes/NotesApp.tsx"
      provides: "Thin plain-text adapter wiring for the existing textarea surface"
    - path: "tests/e2e/notes.spec.ts"
      provides: "Launcher-path proof that Notes still persists, searches, and writes structured durable snapshots"
    - path: "scripts/verify-v1.3.sh"
      provides: "Canonical v1.3 verification command including focused Phase 24 tests"
  key_links:
    - from: "src/features/apps/notes/NotesApp.tsx"
      to: "src/features/apps/notes/notesModel.ts"
      via: "textarea value reads `getNoteBodyText(selectedNote)`"
      pattern: "getNoteBodyText\\(selectedNote\\)"
    - from: "tests/e2e/notes.spec.ts"
      to: "localStorage openos.apps.notes.notes"
      via: "browser assertion inspects version-3 snapshot content after create/reload"
      pattern: "openos\\.apps\\.notes\\.notes"
    - from: "scripts/verify-v1.3.sh"
      to: "src/features/apps/notes/*.test.ts"
      via: "focused state contract test command includes notesContent, notesModel, and notesStorage tests"
      pattern: "notesContent\\.test\\.ts"
---

<objective>
Wire the existing Notes app surface to the structured model and lock the migration into milestone verification.

Purpose: Phase 24 must change the durable model without making the current Notes app look or behave like a new editor surface.
Output: A minimal `NotesApp.tsx` adapter change, browser persistence/search proof, and updated `verify:v1.3` focused test coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
@.planning/phases/24-notes-structured-model-and-migration/24-RESEARCH.md
@.planning/phases/23-state-contracts-and-verification-baseline/23-02-SUMMARY.md
@src/features/apps/notes/notesContent.ts
@src/features/apps/notes/notesModel.ts
@src/features/apps/notes/notesStorage.ts
@src/features/apps/notes/NotesApp.tsx
@tests/e2e/notes.spec.ts
@scripts/verify-v1.3.sh
@package.json

<interfaces>
Use the Plan 24-01/24-02 public surface:
```typescript
export function getNoteBodyText(note: Note): string;

export function updateStoredNote(
  storage: StorageLike,
  namespace: string,
  noteId: string,
  updates: Partial<NoteInput>,
  maybeOptions?: { now?: () => string },
): Note | null;
```

The existing UI must keep these selectors stable:
```text
notes-title-input
notes-body-input
notes-search-input
notes-folder-filter:all
notes-folder-filter:recipes
notes-list
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the textarea surface without adding editor UI</name>
  <files>src/features/apps/notes/NotesApp.tsx</files>
  <read_first>
    - src/features/apps/notes/notesContent.ts
    - src/features/apps/notes/notesModel.ts
    - src/features/apps/notes/notesStorage.ts
    - src/features/apps/notes/NotesApp.tsx
    - .planning/phases/24-notes-structured-model-and-migration/24-CONTEXT.md
  </read_first>
  <action>
    Update `src/features/apps/notes/NotesApp.tsx` only enough to consume the structured `Note` model per D-03 and D-11. Import `getNoteBodyText` from `./notesModel`. Keep `handleUpdateNote(field: "title" | "body", value: string)` and the existing `updateStoredNote` call shape. Change the textarea value from `selectedNote.body` to `getNoteBodyText(selectedNote)`. Do not add toolbar buttons, heading controls, checklist controls, resume/session reads, migration parsing, `JSON.parse`, localStorage key manipulation, Browser tabs, or a global state manager to this component.
  </action>
  <verify>
    <automated>bun run test -- src/features/apps/notes/notesContent.test.ts src/features/apps/notes/notesModel.test.ts src/features/apps/notes/notesStorage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getNoteBodyText" src/features/apps/notes/NotesApp.tsx` finds the textarea adapter import/use.
    - `rg -n "selectedNote\\.body" src/features/apps/notes/NotesApp.tsx` returns no matches.
    - `rg -n "JSON\\.parse|localStorage\\.clear|createAppSessionStorageKey|resetAppSessionSnapshot" src/features/apps/notes/NotesApp.tsx` returns no matches.
    - `bun run test -- src/features/apps/notes/notesContent.test.ts src/features/apps/notes/notesModel.test.ts src/features/apps/notes/notesStorage.test.ts` passes.
  </acceptance_criteria>
  <done>The current title/body editor surface remains plain text while reading/writing structured Notes through existing storage APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock browser behavior and canonical verification</name>
  <files>tests/e2e/notes.spec.ts, scripts/verify-v1.3.sh</files>
  <read_first>
    - tests/e2e/notes.spec.ts
    - scripts/verify-v1.3.sh
    - package.json
    - src/features/apps/notes/notesContent.ts
    - src/features/apps/notes/notesStorage.ts
    - src/features/apps/notes/NotesApp.tsx
  </read_first>
  <behavior>
    - The existing Notes e2e flow still creates folders/notes, searches body text, reloads, and reopens persisted values.
    - The e2e flow additionally inspects `localStorage.getItem("openos.apps.notes.notes")` and sees `version === 3`.
    - The stored note inspected by e2e has `content.version === 1`, at least one paragraph block, and no canonical note-level `body` property.
    - `verify:v1.3` focused tests include `notesContent.test.ts`, `notesModel.test.ts`, and `notesStorage.test.ts`.
  </behavior>
  <action>
    Update `tests/e2e/notes.spec.ts` per D-11 and D-14 by preserving the current launcher-path create/search/folder/reload assertions and adding a post-edit `page.evaluate` assertion against `localStorage.getItem("openos.apps.notes.notes")`. Parse the JSON inside the browser context, assert `snapshot.version` is `3`, find the `Groceries` note, assert `groceriesNote.content.version` is `1`, assert one block has `{ kind: "paragraph", text: "Eggs and milk" }`, and assert `Object.prototype.hasOwnProperty.call(groceriesNote, "body")` is `false`.

    Update `scripts/verify-v1.3.sh` so the focused state contract command includes `src/features/apps/notes/notesContent.test.ts`, `src/features/apps/notes/notesModel.test.ts`, and `src/features/apps/notes/notesStorage.test.ts` alongside the existing platform/runtime state tests. Preserve the full unit suite, typecheck, build, and `bun run test:e2e --project=webkit-iphone` steps.
  </action>
  <verify>
    <automated>bun run verify:v1.3</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "openos\\.apps\\.notes\\.notes|snapshot\\.version|content\\.version|hasOwnProperty\\.call\\(groceriesNote, \"body\"\\)" tests/e2e/notes.spec.ts` finds the durable version-3 browser assertions.
    - `rg -n "notesContent\\.test\\.ts|notesModel\\.test\\.ts|notesStorage\\.test\\.ts" scripts/verify-v1.3.sh` finds focused Phase 24 tests in the canonical command.
    - `bun run verify:v1.3` passes.
  </acceptance_criteria>
  <done>Browser verification proves existing Notes behavior still works and durable writes use structured version-3 snapshots; the canonical milestone gate covers the new focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structured note text -> React textarea/list rendering | User-authored local text is displayed in the app surface. |
| Browser e2e -> durable localStorage assertion | Test code reads the real browser durable payload to prove migration output. |
| Verification command -> milestone quality gate | The shell script decides which focused tests block later phase execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Elevation of Privilege | `NotesApp.tsx` rendering | mitigate | Keep React text rendering and textarea values; do not introduce `dangerouslySetInnerHTML`, raw HTML, or editor framework rendering. |
| T-24-11 | Tampering | `NotesApp.tsx` storage usage | mitigate | Component must call existing storage APIs only; no raw `JSON.parse`, storage key writes, session reset calls, or migration logic in the UI shell. |
| T-24-12 | Tampering | `tests/e2e/notes.spec.ts` | mitigate | Browser-level test asserts the durable payload is version 3 and note-level `body` is absent after normal UI editing. |
| T-24-13 | Repudiation | `scripts/verify-v1.3.sh` | mitigate | Canonical verification logs focused Notes content/model/storage test execution before full tests/typecheck/build/e2e. |
</threat_model>

<verification>
Run the full canonical milestone gate:

```bash
bun run verify:v1.3
```
</verification>

<success_criteria>
- The Notes app still exposes the same visible plain title/body UI and folder/search flow.
- The durable browser payload written through the UI is a version-3 structured Notes snapshot.
- The canonical `verify:v1.3` command includes focused Phase 24 unit tests and passes end to end.
- No rich editor controls, checklist authoring UI, sync/accounts, Browser tabs, or global state manager are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/24-notes-structured-model-and-migration/24-03-SUMMARY.md`
</output>
