---
phase: 04-motion-and-app-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/motion/homeNavigationMotion.ts
  - src/features/motion/homeNavigationMotion.test.ts
  - src/features/motion/supportsViewTransitions.ts
  - src/features/runtime/homeScreenRuntime.ts
  - src/features/runtime/homeScreenRuntime.test.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - MOTN-01
  - MOTN-02
user_setup: []
must_haves:
  truths:
    - Launcher navigation state explicitly models home, opening, open, and closing app states.
    - Motion mode is resolved centrally and can distinguish default vs reduced-motion behavior.
    - The shell can store enough source/target geometry metadata to drive reversible motion without hard-coded layout guesses.
  artifacts:
    - A dedicated motion-state helper exists with unit coverage.
    - View Transition API support detection exists as a separate utility.
    - The shell runtime is wired to the new motion-aware state model.
  key_links:
    - `AdaptiveShellFoundation` reads the motion-aware runtime state instead of only `home` vs `open-app`.
    - Runtime tests cover opening and closing transitions plus reduced-motion mode resolution.
    - Source icon geometry is captured through the motion layer rather than guessed from profile constants.
---

<objective>
Create the shared motion/navigation state foundation for reversible app transitions.

Purpose: Establish one explicit state model for app opening and closing before adding real transition visuals or the Home pill.
Output: Motion-aware runtime state, motion-mode helpers, View Transition support detection, and shell integration with those primitives.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-motion-and-app-navigation/04-CONTEXT.md
@.planning/phases/04-motion-and-app-navigation/04-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/runtime/homeScreenRuntime.ts
@src/features/shell/components/AppIconButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a motion-aware launcher state helper</name>
  <files>src/features/motion/homeNavigationMotion.ts, src/features/motion/homeNavigationMotion.test.ts, src/features/runtime/homeScreenRuntime.ts, src/features/runtime/homeScreenRuntime.test.ts</files>
  <action>Extend the current runtime state into a motion-aware model that explicitly represents `home`, `opening`, `open-app`, and `closing`, including source/target icon geometry and resolved motion mode. Keep the state pure and testable; do not add view styling here.</action>
  <verify>`pnpm test -- --run src/features/motion/homeNavigationMotion.test.ts src/features/runtime/homeScreenRuntime.test.ts` passes.</verify>
  <done>The repo has one explicit motion/navigation state model for open and close behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add feature detection for native view transitions</name>
  <files>src/features/motion/supportsViewTransitions.ts, src/features/motion/homeNavigationMotion.ts</files>
  <action>Add a dedicated utility for View Transition API feature detection and integrate it into the motion helper so the runtime can choose native transitions when supported and fallback behavior otherwise. Keep this detection isolated from the UI.</action>
  <verify>`npx tsc --noEmit` passes and motion tests still pass.</verify>
  <done>Native view transition support is explicit and isolated behind one helper.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the shell runtime to consume motion-aware navigation state</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/runtime/homeScreenRuntime.ts, src/features/motion/homeNavigationMotion.ts</files>
  <action>Update the shell so app opening/closing uses the motion-aware state model instead of a single `open-app` switch. Preserve current behavior visually for now, but ensure the state shape is ready for the next plans to animate it.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The shell is structurally ready for shared open/close transitions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/motion/homeNavigationMotion.test.ts src/features/runtime/homeScreenRuntime.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A single explicit motion/navigation state model governs app open and close
- The shell is structurally ready for shared container animation and Home-button dismissal
</success_criteria>

<output>
After completion, create `.planning/phases/04-motion-and-app-navigation/04-01-SUMMARY.md`
</output>
