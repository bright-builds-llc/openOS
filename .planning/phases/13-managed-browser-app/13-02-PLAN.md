---
phase: 13-managed-browser-app
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/features/apps/browser/BrowserApp.tsx
  - src/features/apps/browser/browser.css
  - src/features/platform/appDefinitions.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - BROW-01
  - BROW-02
  - BROW-03
  - BROW-04
user_setup: []
must_haves:
  truths:
    - `Browser` is now a real implemented app in the shared launcher/runtime path.
    - Users can switch among a small curated set of destinations inside the Browser UI.
    - The Browser app shows a graceful external-fallback state for destinations that should not be embedded.
  artifacts:
    - A real Browser app module exists.
    - The runtime app definitions promote Browser to implemented.
    - Browser UI styling exists for embedded and fallback states.
  key_links:
    - Browser UI consumes the destination registry and iframe host from Plan 01.
    - Browser is routed through the same shared runtime/motion shell as other implemented apps.
    - Fallback behavior is metadata-driven and visible in the Browser UI.
---

<objective>
Implement the Browser app UI and route it through the shared runtime.

Purpose: Deliver Browser as a real implemented app with curated destination switching, embedded browsing for allowed destinations, and graceful external fallback for blocked/non-embeddable ones.
Output: Browser app module, implemented-app routing, and coherent Browser UI for both embedded and fallback states.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-managed-browser-app/13-RESEARCH.md
@src/features/apps/browser/browserDestinations.ts
@src/features/apps/browser/BrowserFrame.tsx
@src/features/platform/appDefinitions.ts
@src/features/runtime/AppSurface.tsx
@src/features/shell/AdaptiveShellFoundation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the real Browser app module and route it through the runtime</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/platform/appDefinitions.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Create the Browser app component and route it through the shared runtime as an implemented app. Keep the app honest about its limited, curated scope rather than implying arbitrary web browsing.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and Browser opens through the normal launcher path.</verify>
  <done>Browser is a real implemented app instead of a coming-soon placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Implement destination switching and embedded browsing UI</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/apps/browser/browser.css</files>
  <action>Implement a Browser UI that lets users switch among the curated destinations, renders embedded destinations through the managed iframe host, and stays visually coherent with openOS. Keep the browsing model intentionally limited and straightforward.</action>
  <verify>Local interaction confirms destination switching works and embed-safe content renders inside the Browser surface.</verify>
  <done>The Browser app delivers the intended limited managed-iframe browsing experience.</done>
</task>

<task type="auto">
  <name>Task 3: Add graceful external fallback behavior for blocked destinations</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/apps/browser/browser.css</files>
  <action>Implement the UI state for destinations marked as external-only or blocked-from-embedding. The fallback should explain why the page opens externally and offer a clear external-open action without feeling like an error crash state.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass after Browser integration.</verify>
  <done>The Browser app stays truthful about embedding limits and handles non-embeddable destinations gracefully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local interaction confirms embedded and fallback destination states both work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser is a real implemented app with curated navigation and graceful fallback
- The app remains truthful about embedding limitations
</success_criteria>

<output>
After completion, create `.planning/phases/13-managed-browser-app/13-02-SUMMARY.md`
</output>
