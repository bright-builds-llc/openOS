---
phase: 11-settings-app
plan: "03"
type: execute
wave: 3
depends_on:
  - "11-02"
files_modified:
  - tests/e2e/settings.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/apps/settings/SettingsApp.tsx
  - .planning/phases/11-settings-app/11-03-SUMMARY.md
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04
user_setup: []
must_haves:
  truths:
    - Browser-level verification proves Settings can be opened, changes a preference, and that the preference persists across relaunch or reload.
    - Settings exposes a first internal app/platform management surface driven by shared metadata.
    - The phase leaves the codebase ready for Notes and Browser to consume the same platform and preference layer.
  artifacts:
    - A focused Settings Playwright spec exists.
    - Settings app-management/info surface is implemented and testable.
    - Any final integration polish required to stabilize Settings behavior is committed.
  key_links:
    - Settings browser verification uses the real launcher/runtime path, not direct component rendering.
    - The app-management surface is driven by shared app metadata from Phase 10.
    - Preference persistence is proven through the same shared store used by the shell.
---

<objective>
Verify Settings end to end and expose the first internal app-management surface.

Purpose: Lock the Settings app behind browser verification and complete the first user-visible platform-management surface that Phase 10 enabled.
Output: Focused Settings browser verification, app-management/info surface, and any small final stabilization fixes required for trustworthy behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-settings-app/11-RESEARCH.md
@src/features/apps/settings/SettingsApp.tsx
@src/features/platform/appSettings.ts
@src/features/runtime/appRegistry.ts
@tests/e2e/fixtures/launcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose the first internal app/platform management surface in Settings</name>
  <files>src/features/apps/settings/SettingsApp.tsx, src/features/runtime/appRegistry.ts</files>
  <action>Add a first internal app/platform management/info section to Settings that is driven by the shared app metadata from Phase 10. Keep it internal-first and informative rather than turning it into a public app marketplace.</action>
  <verify>Local interaction confirms the Settings app-management section is driven by shared app metadata rather than hardcoded app labels.</verify>
  <done>Settings now surfaces the first real platform-management view promised by this milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused Settings browser verification</name>
  <files>tests/e2e/settings.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a Playwright scenario that launches Settings through the real shell path, changes a visible preference, verifies the shell reflects the change, and confirms the preference persists across a reload or relaunch path. Keep the assertions behavior-focused.</action>
  <verify>`pnpm test:e2e -- tests/e2e/settings.spec.ts --project=webkit-iphone` passes.</verify>
  <done>Settings behavior is browser-verified through the real launcher/runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final verification set for the Settings phase</name>
  <files>tests/e2e/settings.spec.ts, src/features/apps/settings/SettingsApp.tsx</files>
  <action>Run the focused Settings browser scenario together with the repo’s baseline checks and make only the small stabilization fixes needed if behavior is clearly within the Settings phase scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The Settings app is stable enough to serve as the first openOS-native control plane for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted Settings Playwright scenario passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings is browser-verified for open/apply/persist behavior
- The first internal app/platform management surface is live in Settings
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-app/11-03-SUMMARY.md`
</output>
