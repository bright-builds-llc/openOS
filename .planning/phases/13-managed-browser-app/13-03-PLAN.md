---
phase: 13-managed-browser-app
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-02"
files_modified:
  - tests/e2e/browser-app.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/apps/browser/BrowserApp.tsx
  - .planning/phases/13-managed-browser-app/13-03-SUMMARY.md
autonomous: true
requirements:
  - BROW-01
  - BROW-02
  - BROW-03
  - BROW-04
user_setup: []
must_haves:
  truths:
    - Browser-level verification proves the Browser app can be opened through the real launcher path.
    - The browser suite proves both embedded-destination behavior and graceful external-fallback behavior.
    - The Browser phase leaves the codebase ready for final integration/verification work in Phase 14.
  artifacts:
    - A focused Browser Playwright spec exists.
    - Browser fallback behavior is browser-verifiable without relying on unstable third-party pages.
    - Any final Browser stabilization fixes required by the scenario are committed.
  key_links:
    - Browser browser-tests use the real launcher/runtime path rather than direct component rendering.
    - Embedded and fallback states are both asserted through the typed destination model from Plan 01.
    - Destination switching is verified through the shipped Browser UI.
---

<objective>
Verify Browser end to end and lock the limited-browser experience.

Purpose: Add focused browser verification for Browser open/navigation/embed/fallback behavior and make only the small stabilization fixes required so the app remains truthful and reliable.
Output: Browser browser verification plus final Browser stabilization inside the shared shell/runtime path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-managed-browser-app/13-RESEARCH.md
@src/features/apps/browser/BrowserApp.tsx
@src/features/apps/browser/browserDestinations.ts
@tests/e2e/fixtures/launcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused Browser browser verification</name>
  <files>tests/e2e/browser-app.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Write a Playwright scenario that launches Browser through the real shell path, switches among curated destinations, verifies embedded content for an embed-safe destination, and verifies the fallback UI/external action for a blocked destination. Keep the assertions behavior-focused and deterministic.</action>
  <verify>`pnpm test:e2e -- tests/e2e/browser-app.spec.ts --project=webkit-iphone` passes.</verify>
  <done>The Browser app is browser-verified instead of relying only on local manual checks.</done>
</task>

<task type="auto">
  <name>Task 2: Make only the small Browser fixes required for stable behavior</name>
  <files>src/features/apps/browser/BrowserApp.tsx, tests/e2e/browser-app.spec.ts</files>
  <action>Make only the minimal Browser-specific adjustments required if the scenario exposes instability in destination switching, embed-safe rendering, or fallback visibility. Do not broaden into general browser features or marketplace work.</action>
  <verify>The focused Browser scenario passes consistently and `pnpm build` remains green.</verify>
  <done>The Browser path feels stable and representative of the intended limited-browser behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final verification set for the Browser phase</name>
  <files>tests/e2e/browser-app.spec.ts, src/features/apps/browser/BrowserApp.tsx</files>
  <action>Run the focused Browser browser scenario together with the repo’s baseline checks and make only the small stabilization fixes needed if behavior is clearly within the Browser phase scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The Browser app is ready as the third real built-in app for `v1.1`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted Browser Playwright scenario passes
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser is browser-verified for open/navigation/embed/fallback behavior
- The limited-browser experience remains truthful and stable enough for Phase 14
</success_criteria>

<output>
After completion, create `.planning/phases/13-managed-browser-app/13-03-SUMMARY.md`
</output>
