---
phase: 21-submitted-manifest-discovery-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/submittedAppManifests.ts
  - src/features/platform/submittedAppManifests.test.ts
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-04-11T21-45-01
generated_at: 2026-04-11T21:45:01.035Z
must_haves:
  truths:
    - The repo can detect drift between submitted manifest files on disk and the registered manifest set consumed by the catalog.
    - Drift detection is testable without relying on the filesystem at every assertion point.
    - The shared submission registry remains the single runtime/catalog source of truth.
  artifacts:
    - A pure helper exists for comparing on-disk manifest files against the registered manifest set.
    - Focused unit coverage exists for missing-on-disk, missing-in-registry, and happy-path comparisons.
    - The submitted manifest module exposes enough metadata for the validator to perform fail-closed drift checks.
  key_links:
    - The catalog continues to consume the shared submitted manifest registry instead of inventing a second data source.
    - The validator can reuse the same registry metadata to detect drift before validating content.
    - Future manifest additions cannot silently bypass both validation and catalog inclusion once the phase is complete.
---

<objective>
Add the shared manifest-registry drift detection model.

Purpose: Create the smallest reusable logic that can prove the submitted manifest registry and the manifest files on disk are in sync before the validator or catalog proceeds.
Output: Drift-detection helper(s), registry metadata needed for comparison, and focused unit coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-CONTEXT.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-RESEARCH.md
@.planning/v1.2-v1.2-MILESTONE-AUDIT.md
@src/features/platform/submittedAppManifests.ts
@src/features/platform/submittedAppManifests.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest-registry drift helpers to the shared submission module</name>
  <files>src/features/platform/submittedAppManifests.ts</files>
  <read_first>
    - src/features/platform/submittedAppManifests.ts
    - scripts/validate-submitted-apps.ts
    - docs/app-submissions.md
    - .planning/phases/21-submitted-manifest-discovery-hardening/21-RESEARCH.md
  </read_first>
  <action>Add pure helper functions in `src/features/platform/submittedAppManifests.ts` that compare the registered manifest ids/source set against a provided list of manifest filenames from `src/features/platform/submitted-apps/`. The helpers must return concrete lists for: files present on disk but missing from the registry, registry entries whose expected JSON file is missing on disk, and the clean no-drift case. Keep the shared exported catalog source `submittedAppManifestRecords` as the single runtime/catalog registry.</action>
  <acceptance_criteria>
    - `src/features/platform/submittedAppManifests.ts` contains an exported helper that compares registered manifests to discovered filenames
    - `src/features/platform/submittedAppManifests.ts` contains logic for both unregistered files and missing backing files
    - `src/features/platform/submittedAppManifests.ts` still exports `submittedAppManifestRecords`
  </acceptance_criteria>
  <verify>`bun run test -- src/features/platform/submittedAppManifests.test.ts` passes after the helper is added.</verify>
  <done>The shared submission module can express registry drift explicitly instead of relying on manual review.</done>
</task>

<task type="auto">
  <name>Task 2: Lock drift-detection behavior with focused unit tests</name>
  <files>src/features/platform/submittedAppManifests.test.ts</files>
  <read_first>
    - src/features/platform/submittedAppManifests.test.ts
    - src/features/platform/submittedAppManifests.ts
    - src/features/platform/submitted-apps/studio-lab.json
    - src/features/platform/submitted-apps/signal-box.json
  </read_first>
  <action>Extend `src/features/platform/submittedAppManifests.test.ts` with focused tests that prove: a clean manifest registry returns no drift issues; an on-disk file name missing from the registry is reported explicitly; and a registered manifest whose expected JSON file is absent from the discovered filename list is reported explicitly. Keep the tests pure by passing concrete filename arrays such as `['studio-lab.json', 'signal-box.json']` into the new helper instead of mocking filesystem APIs.</action>
  <acceptance_criteria>
    - `src/features/platform/submittedAppManifests.test.ts` contains one test for the no-drift case
    - `src/features/platform/submittedAppManifests.test.ts` contains one test that reports an unregistered on-disk file
    - `src/features/platform/submittedAppManifests.test.ts` contains one test that reports a missing backing file for a registered manifest
  </acceptance_criteria>
  <verify>`bun run test -- src/features/platform/submittedAppManifests.test.ts` passes.</verify>
  <done>The drift-detection rules are protected by unit coverage before the CLI validator consumes them.</done>
</task>

</tasks>

<threat_model>
Primary risk: a future submitted manifest file is added on disk but silently omitted from both validation and the catalog, creating hidden drift between review evidence and shipped browse data.
Mitigation required: fail-closed comparison between on-disk manifest filenames and the registered manifest set before content validation or catalog consumption proceeds.
Block on: any path that allows silent omission of an on-disk manifest file.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bun run test -- src/features/platform/submittedAppManifests.test.ts` passes
- [ ] `bun x tsc --noEmit` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared manifest drift can be detected without filesystem-dependent unit tests
- The catalog registry remains the single runtime/catalog source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/21-submitted-manifest-discovery-hardening/21-01-SUMMARY.md`
</output>
