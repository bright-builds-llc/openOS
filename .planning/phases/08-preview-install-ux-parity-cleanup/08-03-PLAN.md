---
phase: 08-preview-install-ux-parity-cleanup
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - tests/e2e/browser-entry.spec.ts
  - tests/e2e/fixtures/launcher.ts
  - src/features/install/browser/PreviewShell.tsx
  - src/features/install/browser/BrowserInstallFlow.tsx
autonomous: true
requirements:
  - INST-01
  - HOME-02
  - HOME-03
  - RUNT-01
user_setup: []
must_haves:
  truths:
    - Browser-mode verification protects preview/runtime parity rather than relying on visual inspection only.
    - Browser-mode verification proves the primary install CTA performs an assistive action.
    - The Phase 8 cleanup does not regress the existing browser-onboarding flow.
  artifacts:
    - A narrow browser-entry Playwright spec exists or the existing browser-mode spec is meaningfully extended.
    - Shared launcher/browser helpers support the new browser-entry assertions cleanly.
    - Any final browser-entry polish required to stabilize the tests is committed.
  key_links:
    - Tests assert behavior that reflects the new preview selector and CTA assist flow, not incidental CSS details.
    - Browser-mode verification remains separate from installed-context verification.
    - Existing browser-entry shell behavior remains compatible with the new parity/assist improvements.
---

<objective>
Lock the preview parity and install-assist cleanup behind narrow verification.

Purpose: Add focused automated coverage for the new Phase 8 promises and make only the small stabilization fixes required for trustworthy browser-entry verification.
Output: Browser-entry verification for preview/runtime parity and install CTA assist behavior, plus any tightly scoped final polish.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1-v1-MILESTONE-AUDIT.md
@.planning/phases/08-preview-install-ux-parity-cleanup/08-RESEARCH.md
@tests/e2e/shell-flow.spec.ts
@tests/e2e/fixtures/launcher.ts
@src/features/install/browser/PreviewShell.tsx
@src/features/install/browser/BrowserInstallFlow.tsx
@src/features/install/browser/BrowserInstallOverlay.tsx
@src/features/install/browser/AppTapIntercept.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add browser-mode verification for preview parity and install-assist behavior</name>
  <files>tests/e2e/browser-entry.spec.ts, tests/e2e/fixtures/launcher.ts</files>
  <action>Add a focused browser-entry Playwright spec, or a tightly scoped extension of the existing browser-mode spec, that proves the preview reflects the shared app source and that tapping the primary install CTA makes install guidance more actionable/visible. Keep the assertions behavior-focused.</action>
  <verify>`pnpm test:e2e -- tests/e2e/browser-entry.spec.ts --project=webkit-iphone` or the equivalent targeted browser-entry command passes.</verify>
  <done>The new Phase 8 promises are protected by narrow browser automation.</done>
</task>

<task type="auto">
  <name>Task 2: Fix only the browser-entry rough edges exposed by the new verification</name>
  <files>src/features/install/browser/PreviewShell.tsx, src/features/install/browser/BrowserInstallFlow.tsx, tests/e2e/browser-entry.spec.ts</files>
  <action>Make only the minimal browser-entry fixes required to keep the new verification reliable and representative. Do not broaden into general shell or install-boundary work.</action>
  <verify>The targeted browser-entry scenarios pass consistently and `pnpm build` remains green.</verify>
  <done>The covered preview and CTA behavior feels intentional enough to trust.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final narrow verification set for the cleanup phase</name>
  <files>tests/e2e/browser-entry.spec.ts, tests/e2e/shell-flow.spec.ts</files>
  <action>Run the browser-entry verification together with the existing browser-mode shell coverage and the repo’s baseline checks. Use the result to lock the optional cleanup phase without re-opening broader milestone scope.</action>
  <verify>`pnpm test`, `pnpm test:e2e --project=webkit-iphone`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The optional cleanup phase is verified as additive browser-entry polish rather than destabilizing churn.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Targeted browser-entry Playwright scenarios pass
- [ ] `pnpm test` passes
- [ ] `pnpm test:e2e --project=webkit-iphone` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preview/runtime parity and install-assist behavior are protected by narrow browser verification
- Existing browser-entry behavior remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/08-preview-install-ux-parity-cleanup/08-03-SUMMARY.md`
</output>
