---
phase: 17-browser-direct-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/browser/browserDestinations.ts
  - src/features/apps/browser/browserDestinations.test.ts
autonomous: true
requirements:
  - BROW-05
  - BROW-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-04-11T18-37-19
generated_at: 2026-04-11T18:37:19.390Z
must_haves:
  truths:
    - Browser direct entry is driven by explicit URL normalization and destination classification logic, not improvised in the component only.
    - Known curated destinations can still be reused when a typed URL resolves to one of them.
    - Unknown external URLs stay truthfully classified as fallback instead of pretending embed support.
  artifacts:
    - A destination helper layer exists for direct URL normalization and classification.
    - Focused unit coverage exists for same-origin direct URLs, normalized bare domains, curated matches, and unsupported protocols.
    - Direct navigation logic remains deterministic enough for browser-path verification later in the phase.
  key_links:
    - Browser direct navigation still hangs off the existing managed Browser surface instead of becoming a separate app.
    - Later UI work can consume direct-navigation helpers without duplicating normalization logic.
    - The truthfulness model stays encoded in executable logic, not only copy or tests.
---

<objective>
Build the direct-navigation destination model for Browser.

Purpose: Establish a deterministic way to accept user-entered addresses, normalize them, and classify them into inline or external-fallback destinations before expanding the Browser UI.
Output: Direct-navigation helper logic plus focused unit coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-browser-direct-navigation/17-CONTEXT.md
@src/features/apps/browser/browserDestinations.ts
@src/features/apps/browser/BrowserApp.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct URL normalization and classification helpers</name>
  <files>src/features/apps/browser/browserDestinations.ts</files>
  <action>Add a narrow helper surface that accepts address-bar input, normalizes it, reuses curated destinations when possible, and classifies same-origin versus external destinations truthfully.</action>
  <verify>`bun run test -- src/features/apps/browser/browserDestinations.test.ts` passes.</verify>
  <done>Browser direct navigation has a deterministic data model the UI can consume.</done>
</task>

<task type="auto">
  <name>Task 2: Keep truthfulness explicit in the direct navigation model</name>
  <files>src/features/apps/browser/browserDestinations.ts</files>
  <action>Ensure unsupported protocols are rejected clearly and unknown external URLs become explicit fallback destinations instead of fake inline pages.</action>
  <verify>The direct-navigation unit tests cover unsupported protocols and external fallback classification.</verify>
  <done>The Browser app stays honest even when destination handling broadens.</done>
</task>

<task type="auto">
  <name>Task 3: Lock focused destination-helper coverage</name>
  <files>src/features/apps/browser/browserDestinations.test.ts</files>
  <action>Add focused unit coverage for same-origin direct paths, normalized bare domains, configured-destination matches, and unsupported protocols. Keep tests centered on behavior, not helper internals.</action>
  <verify>`bun run test` and `bun x tsc --noEmit` pass after the helper changes.</verify>
  <done>The Browser direct-navigation model is stable before UI work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Direct navigation semantics are explicit and truthfulness-preserving
- The Browser UI can reuse configured destinations instead of duplicating them
</success_criteria>

<output>
After completion, create `.planning/phases/17-browser-direct-navigation/17-01-SUMMARY.md`
</output>
