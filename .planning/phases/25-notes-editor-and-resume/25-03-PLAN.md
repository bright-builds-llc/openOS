---
phase: 25-notes-editor-and-resume
plan: "03"
type: execute
wave: 3
depends_on:
  - "25-02"
files_modified:
  - tests/e2e/notes.spec.ts
  - tests/e2e/app-integration.spec.ts
  - tests/e2e/distribution-integration.spec.ts
  - tests/e2e/readme-media.spec.ts
  - scripts/verify-v1.3.sh
autonomous: true
requirements:
  - NOTES-02
  - NOTES-03
  - NOTES-05
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 25-2026-05-31T23-52-26
generated_at: 2026-06-01T00:19:08Z
must_haves:
  truths:
    - "Browser-level coverage proves a user can create structured Notes content with a paragraph, heading, and checklist item."
    - "Browser-level coverage proves a user can toggle checklist content and search/previews still use structured text."
    - "Browser-level coverage proves Notes resumes folder, note, search text, and focused block after home navigation and reload."
    - "The canonical `verify:v1.3` command includes Phase 25 focused unit tests and the full WebKit iPhone launcher-path suite."
  artifacts:
    - path: "tests/e2e/notes.spec.ts"
      provides: "Primary Phase 25 browser proof for structured editing, checklist toggling, session resume, and durable snapshot shape."
    - path: "tests/e2e/app-integration.spec.ts"
      provides: "Integrated launcher-path Notes flow updated from legacy body textarea to structured block input."
    - path: "tests/e2e/distribution-integration.spec.ts"
      provides: "Milestone distribution walkthrough updated to create searchable structured Notes content."
    - path: "tests/e2e/readme-media.spec.ts"
      provides: "README media capture setup updated to use structured Notes block input."
    - path: "scripts/verify-v1.3.sh"
      provides: "Focused v1.3 gate includes `notesSession.test.ts` before full verification."
  key_links:
    - from: "tests/e2e/notes.spec.ts"
      to: "src/features/apps/notes/NotesEditor.tsx"
      via: "stable `notes-add-*` and `notes-block-*` test IDs"
      pattern: "notes-add-heading|notes-block-check"
    - from: "tests/e2e/notes.spec.ts"
      to: "openos.apps.notes.session"
      via: "browser localStorage assertion"
      pattern: "openos\\.apps\\.notes\\.session"
    - from: "scripts/verify-v1.3.sh"
      to: "src/features/apps/notes/notesSession.test.ts"
      via: "focused test list"
      pattern: "notesSession\\.test\\.ts"
---

<objective>
Lock the Phase 25 user behaviors in browser-level tests and make the canonical v1.3 verification command cover the new Notes session unit tests.

Purpose: The model/UI work is only complete when the real launcher path proves structured editing, checklist toggling, and resume after home navigation and reload.
Output: Updated Playwright specs and `verify:v1.3` coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-notes-editor-and-resume/25-CONTEXT.md
@.planning/phases/25-notes-editor-and-resume/25-RESEARCH.md
@.planning/phases/25-notes-editor-and-resume/25-UI-SPEC.md
@.planning/phases/25-notes-editor-and-resume/25-01-SUMMARY.md
@.planning/phases/25-notes-editor-and-resume/25-02-SUMMARY.md
@tests/e2e/fixtures/launcher.ts
@tests/e2e/notes.spec.ts
@tests/e2e/app-integration.spec.ts
@tests/e2e/distribution-integration.spec.ts
@tests/e2e/readme-media.spec.ts
@scripts/verify-v1.3.sh

<interfaces>
Launcher helpers from `tests/e2e/fixtures/launcher.ts`:
```typescript
export async function gotoInstalledContextMode(page: Page): Promise<void>;
export async function openApp(page: Page, appId: string): Promise<void>;
export async function returnHome(page: Page, appId: string): Promise<void>;
export async function waitForHomeScreen(page: Page): Promise<void>;
```

Required Phase 25 test IDs from `25-UI-SPEC.md`:
```text
notes-add-text
notes-add-heading
notes-add-checklist-item
notes-block-input:{index}
notes-block-check:{index}
notes-block-remove:{index}
notes-session-warning
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Notes e2e for structured editing and resume</name>
  <files>tests/e2e/notes.spec.ts</files>
  <read_first>
    - .planning/phases/25-notes-editor-and-resume/25-CONTEXT.md
    - .planning/phases/25-notes-editor-and-resume/25-UI-SPEC.md
    - tests/e2e/fixtures/launcher.ts
    - tests/e2e/notes.spec.ts
    - src/features/apps/notes/NotesEditor.tsx
    - src/features/apps/notes/notesSession.ts
  </read_first>
  <action>Rewrite the existing Notes spec to use structured editor controls instead of `notes-body-input`. Keep the existing installed launcher path, folder creation, search, and reload coverage, then add Phase 25 behavior: create folder `Recipes`; create note `Groceries`; fill `notes-block-input:0` with `Eggs and milk`; click `notes-add-heading` and fill `notes-block-input:1` with `Trip plan`; click `notes-add-checklist-item`, fill `notes-block-input:2` with `Pack charger`, and check `notes-block-check:2`; assert the note list preview/search can find `Pack charger`; inspect `localStorage.getItem("openos.apps.notes.notes")` to assert version 3 durable content includes paragraph, heading, and checked checklist blocks and has no note-level `body`; inspect `localStorage.getItem("openos.apps.notes.session")` to assert version 1 session contains `selectedFolderId`, `selectedNoteId`, non-empty `searchQuery` when meaningful, and `selectedBlockIndex: 2`; call `returnHome(page, "notes")`, reopen Notes, and assert folder `Recipes`, title `Groceries`, search text, `notes-block-input:2`, and checked state resume; reload, wait for home, reopen Notes, and assert the same context again. This browser coverage implements D-16 and also proves structured search/previews from D-07 plus stale or malformed session safety from D-10 through D-12. Add a malformed session setup inside the same spec or a second focused test that seeds `openos.apps.notes.session` with `{bad-json`, reopens Notes, verifies `notes-session-warning` is visible, and verifies durable `Groceries` content remains present.</action>
  <verify>
    <automated>bun run test:e2e --project=webkit-iphone tests/e2e/notes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "notes-add-heading|notes-add-checklist-item|notes-block-input:2|notes-block-check:2|openos\\.apps\\.notes\\.session|notes-session-warning" tests/e2e/notes.spec.ts` returns all Phase 25 assertions.
    - `rg -n "notes-body-input" tests/e2e/notes.spec.ts` returns no matches.
    - `bun run test:e2e --project=webkit-iphone tests/e2e/notes.spec.ts` passes.
  </acceptance_criteria>
  <done>The primary browser spec proves structured editing, checklist toggling, search/previews over checklist text, home navigation resume, reload resume, malformed session reset, and durable/session isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing launcher-path tests from legacy textarea to structured blocks</name>
  <files>tests/e2e/app-integration.spec.ts, tests/e2e/distribution-integration.spec.ts, tests/e2e/readme-media.spec.ts</files>
  <read_first>
    - tests/e2e/app-integration.spec.ts
    - tests/e2e/distribution-integration.spec.ts
    - tests/e2e/readme-media.spec.ts
    - .planning/phases/25-notes-editor-and-resume/25-UI-SPEC.md
    - src/features/apps/notes/NotesEditor.tsx
  </read_first>
  <action>Replace every `notes-body-input` usage in existing e2e tests with structured block authoring. For simple paragraph content, after `notes-create`, fill `notes-block-input:0` with the same text. Where a test searches for body text such as `reviewed metadata`, keep that exact text in `notes-block-input:0` so existing search assertions still prove structured content text is indexed. Do not weaken Browser, Library, Settings, readme media, or launcher-page assertions.</action>
  <verify>
    <automated>bun run test:e2e --project=webkit-iphone tests/e2e/app-integration.spec.ts tests/e2e/distribution-integration.spec.ts tests/e2e/readme-media.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "notes-body-input" tests/e2e` returns no matches.
    - `rg -n "notes-block-input:0" tests/e2e/app-integration.spec.ts tests/e2e/distribution-integration.spec.ts tests/e2e/readme-media.spec.ts` returns structured block usage in all three files.
    - `bun run test:e2e --project=webkit-iphone tests/e2e/app-integration.spec.ts tests/e2e/distribution-integration.spec.ts tests/e2e/readme-media.spec.ts` passes.
  </acceptance_criteria>
  <done>All existing browser tests that create Notes content use the structured editor contract and no spec depends on the removed legacy body textarea.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 25 tests to verify:v1.3 and run the canonical gate</name>
  <files>scripts/verify-v1.3.sh</files>
  <read_first>
    - scripts/verify-v1.3.sh
    - package.json
    - src/features/apps/notes/notesSession.test.ts
    - .planning/phases/25-notes-editor-and-resume/25-CONTEXT.md
  </read_first>
  <action>Add `src/features/apps/notes/notesSession.test.ts` to the focused state contract test list in `scripts/verify-v1.3.sh` after the existing Notes storage/content/model focused tests. Keep the existing order of submission validation, focused tests, full unit suite, explicit typecheck, production build, and full WebKit iPhone launcher-path suite. Do not remove `verify:v1.2`, do not add a second v1.3 command, and do not skip the full e2e suite.</action>
  <verify>
    <automated>bun run verify:v1.3</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "notesSession\\.test\\.ts" scripts/verify-v1.3.sh` returns the focused Phase 25 session test.
    - `rg -n "submissions:check|bun run test --|bun x tsc --noEmit|bun run build|test:e2e --project=webkit-iphone" scripts/verify-v1.3.sh` returns all existing verification stages.
    - `bun run verify:v1.3` passes.
  </acceptance_criteria>
  <done>The canonical v1.3 verification command proves Phase 25 unit coverage and the full launcher-path regression suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser automation -> app UI | Playwright drives user-like editing, toggling, search, home navigation, and reload flows through public UI controls. |
| Browser localStorage inspection -> assertions | Tests inspect durable and session snapshots to prove exact key separation and parser-safe envelope shapes. |
| Verification script -> repo quality gate | `verify:v1.3` determines whether Phase 25 behavior is release-ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Tampering | `tests/e2e/notes.spec.ts` | mitigate | Assert durable `openos.apps.notes.notes` keeps version 3 structured content and session `openos.apps.notes.session` keeps only disposable UI hints. |
| T-25-03-02 | Elevation of privilege | user-authored note rendering | mitigate | Browser tests create text containing normal user-authored content through inputs and never require HTML rendering; grep removes legacy textarea-only coverage. |
| T-25-03-03 | Denial of service | malformed session snapshots | mitigate | Browser test seeds malformed session JSON, verifies warning UI, and proves durable Notes content remains visible. |
| T-25-03-04 | Repudiation | `scripts/verify-v1.3.sh` | mitigate | Keep a single canonical command that runs focused tests, full unit tests, typecheck, build, and full WebKit iPhone e2e so Phase 25 completion has reproducible evidence. |
</threat_model>

<verification>
Run targeted e2e after test updates:

```bash
bun run test:e2e --project=webkit-iphone tests/e2e/notes.spec.ts
bun run test:e2e --project=webkit-iphone tests/e2e/app-integration.spec.ts tests/e2e/distribution-integration.spec.ts tests/e2e/readme-media.spec.ts
```

Then run the canonical gate:

```bash
bun run verify:v1.3
```
</verification>

<success_criteria>
- No e2e test references `notes-body-input`.
- `tests/e2e/notes.spec.ts` proves structured paragraph, heading, and checked checklist blocks in the durable snapshot.
- `tests/e2e/notes.spec.ts` proves session snapshot shape and resume after both home navigation and reload.
- Malformed Notes session state produces `notes-session-warning` and leaves durable Notes content visible.
- `scripts/verify-v1.3.sh` includes `notesSession.test.ts` and `bun run verify:v1.3` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/25-notes-editor-and-resume/25-03-SUMMARY.md`
</output>
