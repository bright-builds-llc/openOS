---
phase: 03-home-screen-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/runtime/appRegistry.ts
  - src/features/runtime/appRegistry.test.ts
  - src/features/runtime/homeScreenRuntime.ts
  - src/features/runtime/homeScreenRuntime.test.ts
  - src/features/shell/data/homeScreenIcons.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
user_setup: []
must_haves:
  truths:
    - The home screen is driven by an explicit app registry rather than static shell-only icon arrays.
    - Dock and grid placement are explicit properties of runtime app metadata.
    - Runtime app lookup and open-state transitions are testable without rendering the whole UI.
  artifacts:
    - A typed app registry module exists with availability and placement semantics.
    - A pure runtime state helper exists with unit coverage.
    - The shell scene consumes runtime-backed app data instead of standalone placeholder arrays.
  key_links:
    - `AdaptiveShellFoundation` derives visible shell app content from the runtime registry.
    - Runtime tests cover launch lookup and state transitions.
    - The registry marks Calculator as implemented and dock apps explicitly as dock entries.
---

<objective>
Create the registry and runtime foundation for a real launcher.

Purpose: Establish a single source of truth for app identity, placement, and availability before interaction behavior and app surfaces deepen.
Output: Typed app registry, pure launcher state helpers, and shell integration with runtime-backed app data.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-home-screen-runtime/03-CONTEXT.md
@.planning/phases/03-home-screen-runtime/03-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/data/homeScreenIcons.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed app registry</name>
  <files>src/features/runtime/appRegistry.ts, src/features/runtime/appRegistry.test.ts, src/features/shell/data/homeScreenIcons.ts</files>
  <action>Create a typed runtime app model with the locked fields: `id`, `label`, `icon`, `placement`, `availability`, and `launch surface`. Seed it from the existing shell icon inventory so shell presentation and runtime identity do not diverge. Explicitly mark Calculator as the only `implemented` app and the dock apps as explicit dock entries.</action>
  <verify>`pnpm test -- --run src/features/runtime/appRegistry.test.ts` passes.</verify>
  <done>The repo has one explicit source of truth for app identity, placement, and availability.</done>
</task>

<task type="auto">
  <name>Task 2: Add a minimal launcher runtime state helper</name>
  <files>src/features/runtime/homeScreenRuntime.ts, src/features/runtime/homeScreenRuntime.test.ts</files>
  <action>Implement a pure runtime helper that models the launcher in the simplest phase-appropriate terms: `home` or `open-app`. Include lookup helpers so the UI can open either implemented or placeholder apps without shell-specific branching. Do not introduce motion state or persistence in this phase.</action>
  <verify>`pnpm test -- --run src/features/runtime/homeScreenRuntime.test.ts` passes.</verify>
  <done>Runtime transitions and app lookup are explicit, typed, and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the shell to consume runtime app data</name>
  <files>src/features/shell/AdaptiveShellFoundation.tsx, src/features/shell/data/homeScreenIcons.ts</files>
  <action>Replace shell-only icon assumptions with runtime-backed app data. Keep the shell visually unchanged in intent, but make the grid and dock derive from the app registry so launch behavior and presentation share one model.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The shell scene is now backed by runtime app metadata rather than disconnected presentation arrays.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test -- --run src/features/runtime/appRegistry.test.ts` passes
- [ ] `pnpm test -- --run src/features/runtime/homeScreenRuntime.test.ts` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A single runtime registry governs visible apps and launchability
- Runtime state stays simple enough for Phase 4 to add motion later without refactoring
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-screen-runtime/03-01-SUMMARY.md`
</output>
