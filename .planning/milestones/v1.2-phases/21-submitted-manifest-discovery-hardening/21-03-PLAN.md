---
phase: 21-submitted-manifest-discovery-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "21-02"
files_modified:
  - docs/app-submissions.md
  - README.md
  - scripts/verify-v1.2.sh
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-04-11T21-45-01
generated_at: 2026-04-11T21:45:01.035Z
must_haves:
  truths:
    - Contributor docs match the hardened manifest workflow and no longer imply that adding a JSON file alone is sufficient.
    - The milestone verification path still passes with the hardened submission workflow in place.
    - The hardened submission workflow remains discoverable from the normal repo entrypoints.
  artifacts:
    - `docs/app-submissions.md` documents the drift-failure behavior and the full registration/validation path.
    - Any final verification entrypoint that references submission validation still remains accurate.
    - The full repo verification set passes after the hardening work lands.
  key_links:
    - Contributor docs reference the same validator behavior introduced in Plan 02.
    - The hardened manifest workflow remains part of the canonical milestone verification path.
    - Phase 21 closes the audit debt without changing catalog/install product scope.
---

<objective>
Close out the hardened manifest workflow and verify it across the repo.

Purpose: Update contributor guidance to match the hardened manifest workflow and prove the repo still passes its normal verification surface after the submission-path hardening lands.
Output: Documentation updates plus final verification for the hardened manifest workflow.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-CONTEXT.md
@.planning/phases/21-submitted-manifest-discovery-hardening/21-RESEARCH.md
@docs/app-submissions.md
@README.md
@scripts/verify-v1.2.sh
@scripts/validate-submitted-apps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update contributor docs for the hardened manifest workflow</name>
  <files>docs/app-submissions.md, README.md</files>
  <read_first>
    - docs/app-submissions.md
    - README.md
    - scripts/validate-submitted-apps.ts
    - .planning/v1.2-v1.2-MILESTONE-AUDIT.md
  </read_first>
  <action>Update `docs/app-submissions.md` so the workflow explicitly says contributors must both add the JSON file and keep the shared submission registry in sync, and document that `bun run submissions:check` now fails when on-disk manifest files and the registered set diverge. Update `README.md` only if the submission foundations section or final verification guidance needs wording that reflects the hardened workflow.</action>
  <acceptance_criteria>
    - `docs/app-submissions.md` contains language that the validator fails when manifest files and the shared registry diverge
    - `docs/app-submissions.md` still documents `bun run submissions:check`
    - if `README.md` is modified, it remains consistent with the hardened submission workflow
  </acceptance_criteria>
  <verify>Reading the docs is sufficient to understand the hardened manifest registration path.</verify>
  <done>Contributor instructions no longer understate the manifest registration requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Keep the canonical verification path accurate</name>
  <files>scripts/verify-v1.2.sh, README.md</files>
  <read_first>
    - scripts/verify-v1.2.sh
    - README.md
    - scripts/validate-submitted-apps.ts
  </read_first>
  <action>Adjust `scripts/verify-v1.2.sh` or related docs only if the hardened submission validator changes any assumptions about the final verification path. Preserve the command order: submissions check, tests, explicit typecheck, build, and full WebKit launcher-path suite.</action>
  <acceptance_criteria>
    - `scripts/verify-v1.2.sh` still contains `bun run submissions:check`
    - `scripts/verify-v1.2.sh` still contains `bun run test:e2e --project=webkit-iphone`
    - the final verification command remains `bun run verify:v1.2`
  </acceptance_criteria>
  <verify>`bun run verify:v1.2` remains the canonical verification entrypoint.</verify>
  <done>The hardened submission workflow stays integrated into the repo’s canonical quality gate.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full verification set for the hardened workflow</name>
  <files>docs/app-submissions.md, scripts/validate-submitted-apps.ts, scripts/verify-v1.2.sh</files>
  <read_first>
    - scripts/validate-submitted-apps.ts
    - scripts/verify-v1.2.sh
    - docs/app-submissions.md
  </read_first>
  <action>Run `bun run submissions:check`, `bun run test`, `bun x tsc --noEmit`, and `bun run build`. If the hardened submission workflow changes anything in the final verification path, also run `bun run verify:v1.2` before closing the phase. Only make small stabilization fixes if the hardened workflow itself causes a failure.</action>
  <acceptance_criteria>
    - repository verification notes cite `bun run submissions:check`
    - repository verification notes cite `bun run test`
    - repository verification notes cite `bun x tsc --noEmit`
    - repository verification notes cite `bun run build`
  </acceptance_criteria>
  <verify>`bun run submissions:check`, `bun run test`, `bun x tsc --noEmit`, and `bun run build` all pass.</verify>
  <done>The hardened manifest workflow is proven clean against the repo’s normal verification surface.</done>
</task>

</tasks>

<threat_model>
Primary risk: docs and final verification guidance lag behind the hardened validator behavior, leaving future contributors with an inaccurate submission workflow.
Mitigation required: align docs with the fail-closed validator and re-run the standard verification surface after the hardening changes land.
Block on: any documentation or verification path that still implies silent manifest omission is acceptable.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bun run submissions:check` passes
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contributor docs accurately describe the hardened manifest workflow
- The hardened submission path remains part of the repo’s canonical verification surface
</success_criteria>

<output>
After completion, create `.planning/phases/21-submitted-manifest-discovery-hardening/21-03-SUMMARY.md`
</output>
