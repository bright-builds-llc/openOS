---
phase: 04-motion-and-app-navigation
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/features/motion/MotionLayer.tsx
  - src/features/motion/motionNavigation.css
  - src/features/shell/components/AppIconButton.tsx
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - MOTN-01
user_setup: []
must_haves:
  truths:
    - Opening an app uses a shared container transition that starts from the tapped icon’s position.
    - The same outer open transition is used for Calculator and placeholder apps.
    - Reduced-motion mode uses a simpler, gentler open path without changing navigation semantics.
  artifacts:
    - A dedicated motion layer exists for app-open transitions.
    - Motion CSS exists separately from shell composition CSS.
    - App icon interaction captures source icon geometry for transitions.
  key_links:
    - `AppIconButton` passes source icon geometry into the motion layer.
    - `AdaptiveShellFoundation` routes all app opens through the shared motion layer.
    - Reduced-motion mode changes the visual treatment but not the state transitions.
---

<objective>
Implement the shared app-open transition from tapped icon to full-screen app surface.

Purpose: Make app launch feel like part of the OS illusion instead of an instant content swap.
Output: Shared open-transition layer, source-icon geometry capture, and reduced-motion-aware open behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-motion-and-app-navigation/04-CONTEXT.md
@.planning/phases/04-motion-and-app-navigation/04-RESEARCH.md
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/shell/components/AppIconButton.tsx
@src/features/runtime/AppSurface.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared motion layer for app opening</name>
  <files>src/features/motion/MotionLayer.tsx, src/features/motion/motionNavigation.css, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Create one shared transition layer responsible for rendering the open-app container transition. Use View Transition API when available and a transform/opacity fallback otherwise, but keep the visual direction near-literal and uniform across app types.</action>
  <verify>`pnpm build` passes and the shell can open apps through the new transition layer.</verify>
  <done>The launcher has one shared container motion path for app opening.</done>
</task>

<task type="auto">
  <name>Task 2: Capture tapped-icon geometry and hand off from pressed state to motion</name>
  <files>src/features/shell/components/AppIconButton.tsx, src/features/motion/MotionLayer.tsx, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Capture the tapped icon’s `getBoundingClientRect()` at interaction time, preserve the short pressed-state feedback, and pass the geometry into the motion layer so the open animation visibly originates from the tapped icon rather than a generic center zoom.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>App opening originates from the tapped icon with the pressed-state handoff preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the reduced-motion open fallback</name>
  <files>src/features/motion/motionNavigation.css, src/features/motion/MotionLayer.tsx</files>
  <action>Use the resolved motion mode to simplify the open transition to a very short fade/minimal scale shift when reduced motion is requested. Do not alter app-open semantics or routing behavior.</action>
  <verify>Reduced-motion branch is present in code/CSS and `pnpm build` still passes.</verify>
  <done>The open transition respects reduced motion without changing navigation behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell check confirms app opening originates from the tapped icon and reduced-motion fallback is gentler
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- App open transitions feel near-literal and shared across app types
- Reduced motion simplifies the visual travel but preserves runtime behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-motion-and-app-navigation/04-02-SUMMARY.md`
</output>
