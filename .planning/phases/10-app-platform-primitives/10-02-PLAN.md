---
phase: 10-app-platform-primitives
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/features/platform/appSettings.ts
  - src/features/platform/appStorage.ts
  - src/features/platform/appSettings.test.ts
  - src/features/platform/appStorage.test.ts
  - src/features/runtime/appRegistry.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - Apps can explicitly declare settings participation and storage namespace metadata through shared platform primitives.
    - Storage namespace conventions are no longer implicit or scattered across future app code.
    - Internal app-management selectors can query that metadata without each app inventing its own wiring.
  artifacts:
    - A shared settings-participation primitive exists.
    - A shared storage-namespace primitive exists.
    - Focused tests protect both metadata contracts and selector behavior.
  key_links:
    - Runtime app definitions carry settings/storage metadata through the same shared platform layer.
    - Future Settings and Notes work will be able to query settings-visible apps and storage namespaces through stable helpers.
    - The new metadata remains internal-first and does not require public app distribution surfaces.
---

<objective>
Define settings participation and storage namespace primitives.

Purpose: Extend the new app-definition layer so future apps can declare settings participation and storage conventions through shared internal metadata rather than ad hoc implementation choices.
Output: Settings/storage platform modules, registry metadata integration, and focused tests for the new contracts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-app-platform-primitives/10-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/install/browser/installPromptStorage.ts
@src/features/install/standalone/LaunchStateStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared settings-participation primitive</name>
  <files>src/features/platform/appSettings.ts, src/features/platform/appSettings.test.ts, src/features/runtime/appRegistry.ts</files>
  <action>Create a lightweight internal settings-participation model that lets apps declare whether and how they appear in future Settings/app-management surfaces. Integrate it into the app-definition/registry layer without adding actual Settings UI yet.</action>
  <verify>`pnpm test` passes with focused coverage for settings-participation selectors.</verify>
  <done>The codebase has a stable internal contract for app settings participation.</done>
</task>

<task type="auto">
  <name>Task 2: Add a shared storage-namespace primitive</name>
  <files>src/features/platform/appStorage.ts, src/features/platform/appStorage.test.ts, src/features/runtime/appRegistry.ts</files>
  <action>Create a shared storage-namespace convention for app-local persistence and integrate it into the app-definition/registry layer. Keep the primitive simple and future-facing so Notes can consume it without reworking platform assumptions later.</action>
  <verify>`pnpm test` passes with focused coverage for storage-namespace helpers and invariants.</verify>
  <done>The codebase has an explicit storage namespace convention for future app persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Expose internal app-management selectors through the platform layer</name>
  <files>src/features/platform/appSettings.ts, src/features/platform/appStorage.ts, src/features/runtime/appRegistry.ts</files>
  <action>Expose selectors/helpers that future phases can use to list settings-visible apps and resolve app storage namespaces from the shared platform layer. Do not broaden into user-facing app management UI in this phase.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the metadata integration.</verify>
  <done>Future app phases can consume stable platform selectors instead of inventing their own wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Apps can declare settings participation and storage namespace metadata through shared primitives
- Future phases have stable internal selectors for app management and storage lookup
</success_criteria>

<output>
After completion, create `.planning/phases/10-app-platform-primitives/10-02-SUMMARY.md`
</output>
