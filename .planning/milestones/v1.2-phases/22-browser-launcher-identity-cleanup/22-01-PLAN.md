---
phase: 22-browser-launcher-identity-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/appStorage.ts
  - src/features/platform/appStorage.test.ts
  - src/features/platform/appDefinitions.ts
  - src/features/platform/appDefinitions.test.ts
  - src/features/runtime/appRegistry.test.ts
autonomous: true
requirements: []
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-04-12T01-21-17
generated_at: 2026-04-12T01:21:17.070Z
must_haves:
  truths:
    - Browser grid and dock launcher entries resolve to one explicit shared Browser storage namespace.
    - Existing launcher ids and placements remain intact while Browser app-side identity becomes safer for future persistence.
    - Canonical runtime selectors and focused metadata tests reflect the cleaned-up Browser identity.
  artifacts:
    - The shared app-storage helper can express a deliberate namespace alias when launcher id and storage identity differ.
    - Browser launcher definitions use that explicit alias so both entries point at the same Browser namespace.
    - Focused unit coverage proves both per-entry and canonical Browser storage lookups now return the shared namespace.
  key_links:
    - Shared app-definition metadata remains the source of truth for Browser storage ownership.
    - Runtime selectors keep Browser deduped by launch surface and now report coherent storage metadata.
    - Future Browser persistence work can rely on shared metadata without accidentally splitting grid and dock state.
---

<objective>
Make the Browser storage identity explicit and future-safe in the shared platform/runtime metadata.

Purpose: Remove the divergent Browser storage namespace risk while preserving the existing launcher model and canonical runtime selectors.
Output: Shared Browser storage aliasing in the platform layer plus focused metadata tests that lock the behavior in place.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-v1.2-MILESTONE-AUDIT.md
@.planning/phases/22-browser-launcher-identity-cleanup/22-CONTEXT.md
@src/features/platform/appStorage.ts
@src/features/platform/appDefinitions.ts
@src/features/runtime/appRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the Browser storage alias explicit in shared platform metadata</name>
  <files>src/features/platform/appStorage.ts, src/features/platform/appDefinitions.ts</files>
  <read_first>
    - src/features/platform/appStorage.ts
    - src/features/platform/appDefinitions.ts
    - src/features/runtime/appRegistry.ts
    - .planning/v1.2-v1.2-MILESTONE-AUDIT.md
  </read_first>
  <action>Adjust the shared app-storage helper so a launcher entry can deliberately reuse another app identity's namespace when that reflects one real implemented app. Use that mechanism to make both Browser launcher entries point at the same shared Browser storage namespace while keeping the existing `browser-grid` and `browser` launcher ids and placements intact.</action>
  <acceptance_criteria>
    - `src/features/platform/appStorage.ts` can express an explicit storage namespace alias
    - `src/features/platform/appDefinitions.ts` maps both Browser launcher entries onto the same Browser storage namespace
    - Browser launch-surface wiring remains unchanged
  </acceptance_criteria>
  <verify>`bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appDefinitions.test.ts src/features/runtime/appRegistry.test.ts` passes.</verify>
  <done>The shared platform metadata no longer risks split Browser persistence across grid and dock launchers.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the Browser identity cleanup with focused metadata tests</name>
  <files>src/features/platform/appStorage.test.ts, src/features/platform/appDefinitions.test.ts, src/features/runtime/appRegistry.test.ts</files>
  <read_first>
    - src/features/platform/appStorage.test.ts
    - src/features/platform/appDefinitions.test.ts
    - src/features/runtime/appRegistry.test.ts
    - src/features/platform/appDefinitions.ts
  </read_first>
  <action>Update the focused platform/runtime tests so they prove the Browser grid entry, dock entry, and canonical runtime selectors all report the same Browser storage namespace. Keep the assertions centered on metadata truthfulness rather than generic Browser UI behavior.</action>
  <acceptance_criteria>
    - `appStorage.test.ts` proves the alias path explicitly
    - `appDefinitions.test.ts` proves Browser grid and dock entries share the same storage namespace
    - `appRegistry.test.ts` proves both per-entry and canonical Browser namespace lookups now agree
  </acceptance_criteria>
  <verify>`bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appDefinitions.test.ts src/features/runtime/appRegistry.test.ts` passes.</verify>
  <done>The Browser identity cleanup is protected by focused metadata coverage.</done>
</task>

</tasks>

<threat_model>
Primary risk: a future Browser persistence or history feature could accidentally split state between the grid and dock launchers because the shared Browser experience still advertises two storage namespaces today.
Mitigation required: make the Browser storage alias explicit in shared metadata and prove the selectors report one coherent namespace.
Block on: any change that preserves divergent Browser namespaces or hides the aliasing behind implicit behavior.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `bun run test -- src/features/platform/appStorage.test.ts src/features/platform/appDefinitions.test.ts src/features/runtime/appRegistry.test.ts` passes
- [ ] `bun x tsc --noEmit` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser grid and dock entries now expose one explicit shared storage namespace
- Runtime metadata tests make the Browser identity cleanup obvious
</success_criteria>

<output>
After completion, create `.planning/phases/22-browser-launcher-identity-cleanup/22-01-SUMMARY.md`
</output>
