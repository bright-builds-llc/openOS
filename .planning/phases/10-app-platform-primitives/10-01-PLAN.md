---
phase: 10-app-platform-primitives
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/platform/appDefinitions.ts
  - src/features/runtime/appRegistry.ts
  - src/features/shell/data/homeScreenIcons.ts
  - src/features/platform/appDefinitions.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - Built-in app metadata is defined through shared platform primitives rather than raw ad hoc objects only.
    - Existing runtime registry consumers still work after the registry is refactored onto the new definitions.
    - The platform layer remains internal-first and narrowly scoped to built-in apps.
  artifacts:
    - A shared app-definition/platform metadata module exists.
    - The runtime registry consumes that module instead of duplicating the definition shape inline.
    - Focused tests protect the new app-definition layer.
  key_links:
    - App identity, page placement, and launch-surface metadata flow from the shared definitions into the runtime registry.
    - Existing shell selectors consume the same registry output after the refactor.
    - The platform layer is small and directly connected to shipped launcher code.
---

<objective>
Create the shared internal app-definition layer.

Purpose: Replace ad hoc built-in app objects with a reusable app-definition/platform metadata layer that later Settings, Notes, and Browser work can build on.
Output: Shared app-definition module, registry refactor, and focused definition tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/10-app-platform-primitives/10-RESEARCH.md
@src/features/runtime/appRegistry.ts
@src/features/shell/data/homeScreenIcons.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared built-in app definition module</name>
  <files>src/features/platform/appDefinitions.ts, src/features/platform/appDefinitions.test.ts</files>
  <action>Create the first internal app-platform primitive layer for built-in app definitions. It should express app identity and existing launcher metadata cleanly, stay internal-first, and be small enough that it does not turn into speculative framework work.</action>
  <verify>`pnpm test` passes with focused coverage for the new app-definition behavior.</verify>
  <done>The codebase has one shared internal shape for defining built-in apps.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the runtime registry to consume the shared app definitions</name>
  <files>src/features/runtime/appRegistry.ts, src/features/platform/appDefinitions.ts</files>
  <action>Refactor the existing runtime registry onto the new shared app-definition layer without changing current launcher behavior. Keep the resulting registry easy for later phases to consume.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass after the refactor.</verify>
  <done>The shared platform primitive is part of the real launcher path instead of sitting unused.</done>
</task>

<task type="auto">
  <name>Task 3: Keep shell selectors aligned with the refactored registry output</name>
  <files>src/features/shell/data/homeScreenIcons.ts, src/features/runtime/appRegistry.ts</files>
  <action>Adjust the existing shell data selectors only as needed so they continue to consume the refactored registry output cleanly. Do not broaden into app-specific or settings-specific behavior yet.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass.</verify>
  <done>The platform definition layer is wired into current runtime and shell consumers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Built-in apps are defined through a shared internal app-definition primitive
- Existing launcher consumers remain intact after the refactor
</success_criteria>

<output>
After completion, create `.planning/phases/10-app-platform-primitives/10-01-SUMMARY.md`
</output>
