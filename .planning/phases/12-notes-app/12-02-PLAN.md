---
phase: 12-notes-app
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - src/features/apps/notes/NotesApp.tsx
  - src/features/apps/notes/notes.css
  - src/features/runtime/appRegistry.ts
  - src/features/shell/AdaptiveShellFoundation.tsx
  - src/features/platform/appDefinitions.ts
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-04
user_setup: []
must_haves:
  truths:
    - `Notes` is now a real implemented app in the shared launcher/runtime path.
    - Users can create, edit, delete, and reopen notes through the Notes UI.
    - The app clearly communicates that notes are local-only and not synced.
  artifacts:
    - A real Notes app module exists.
    - The runtime registry marks Notes implemented and routes it through its own app surface.
    - Notes UI styling exists and supports list + editor flow coherently.
  key_links:
    - Notes UI consumes the repository/storage layer from Plan 01.
    - Notes is routed through the same shared runtime/motion shell as other implemented apps.
    - Local-only/no-sync messaging is part of the primary UI, not buried or omitted.
---

<objective>
Implement the Notes app UI on top of the local repository layer.

Purpose: Deliver Notes as a real implemented app with straightforward local CRUD flow and honest local-only messaging, using the repository/storage path established in Plan 01.
Output: Notes app module, implemented-app routing, and coherent local-only notes UI.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-notes-app/12-RESEARCH.md
@src/features/apps/notes/notesStorage.ts
@src/features/runtime/appRegistry.ts
@src/features/runtime/AppSurface.tsx
@src/features/shell/AdaptiveShellFoundation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the real Notes app module and route it through the runtime</name>
  <files>src/features/apps/notes/NotesApp.tsx, src/features/platform/appDefinitions.ts, src/features/shell/AdaptiveShellFoundation.tsx</files>
  <action>Create the `Notes` app component and route it through the shared runtime as an implemented app. Keep the app flow focused on the milestone’s local CRUD scope and avoid rich-text/folder/sync expansion.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass, and Notes opens through the normal launcher path.</verify>
  <done>Notes is a real implemented app instead of a coming-soon placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Implement list + editor + local-only warning flow</name>
  <files>src/features/apps/notes/NotesApp.tsx, src/features/apps/notes/notes.css</files>
  <action>Implement a coherent Notes UI with note list, create/open/edit/delete behavior, and visible local-only/no-sync messaging. The warning should be clear and integrated into the app flow, not hidden in a footnote.</action>
  <verify>Local interaction confirms note CRUD works and the local-only warning is plainly visible.</verify>
  <done>The Notes app delivers the intended local-only user experience for this milestone.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure saved notes reopen correctly across relaunches</name>
  <files>src/features/apps/notes/NotesApp.tsx, src/features/apps/notes/notesStorage.ts</files>
  <action>Make any small integration fixes needed so saved notes are listed and reopen correctly after reload/relaunch through the existing local repository path. Keep this tightly scoped to the current local-only Notes flow.</action>
  <verify>`pnpm test`, `npx tsc --noEmit`, and `pnpm build` all pass after Notes integration.</verify>
  <done>The Notes app persists and reopens local notes correctly on top of the shared storage layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local interaction confirms saved notes reopen correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Notes is a real implemented app with local-only CRUD and visible no-sync messaging
- Notes persistence behaves correctly through the shared repository layer
</success_criteria>

<output>
After completion, create `.planning/phases/12-notes-app/12-02-SUMMARY.md`
</output>
