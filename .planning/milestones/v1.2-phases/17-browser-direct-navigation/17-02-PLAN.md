---
phase: 17-browser-direct-navigation
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - src/features/apps/browser/BrowserApp.tsx
  - src/features/apps/browser/BrowserFrame.tsx
  - src/features/apps/browser/browser.css
  - src/features/apps/browser/browserFrame.css
autonomous: true
requirements:
  - BROW-05
  - BROW-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-04-11T18-37-19
generated_at: 2026-04-11T18:37:19.390Z
must_haves:
  truths:
    - Browser exposes a real address-entry flow inside the current app surface.
    - Users can navigate beyond the original shortcut list while still seeing clear distinction between inline and fallback destinations.
    - The expanded Browser still feels coherent inside the existing launcher/runtime path.
  artifacts:
    - Browser UI includes an address bar and a direct navigation action.
    - Suggested curated destinations still exist alongside broader direct navigation.
    - The Browser frame and fallback surfaces visibly reflect the current address.
  key_links:
    - Browser UI consumes the direct-navigation helper layer from Plan 01.
    - The current Browser app remains the single managed browsing surface instead of splitting into separate modes.
    - Truthfulness copy stays embedded in the Browser UI as the navigation model broadens.
---

<objective>
Implement direct URL entry inside the Browser app UI.

Purpose: Turn the direct-navigation model into a user-facing Browser flow with address entry, suggested shortcuts, and honest fallback behavior that still feels like the same managed Browser app.
Output: Address bar UI, broadened destination handling, and coherent Browser styling.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-browser-direct-navigation/17-CONTEXT.md
@src/features/apps/browser/BrowserApp.tsx
@src/features/apps/browser/browser.css
@src/features/apps/browser/browserDestinations.ts
@src/features/apps/browser/BrowserFrame.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add address entry and direct navigation controls</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/apps/browser/browser.css</files>
  <action>Add an address-entry flow with a Go action, show clear validation feedback for invalid inputs, and keep the current curated destinations accessible as suggested shortcuts.</action>
  <verify>Local interaction confirms the Browser app accepts typed URLs and switches surfaces accordingly.</verify>
  <done>Browser no longer depends on shortcut tiles alone for navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Show the current address clearly in inline and fallback views</name>
  <files>src/features/apps/browser/BrowserApp.tsx, src/features/apps/browser/BrowserFrame.tsx, src/features/apps/browser/browserFrame.css</files>
  <action>Ensure the current destination address is visible whether the app is rendering inline or routing to Safari fallback, so the Browser stays understandable and truthful.</action>
  <verify>The current address is visible in the Browser UI for both embedded and fallback destinations.</verify>
  <done>Users can tell what destination the Browser is currently handling.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the expanded Browser visually coherent on portrait mobile sizes</name>
  <files>src/features/apps/browser/browser.css</files>
  <action>Extend the Browser styling for the address bar, broadened destination handling, and any lightweight recent-destination surface while preserving the current built-in app feel.</action>
  <verify>`bun x tsc --noEmit` and `bun run build` pass after the Browser UI changes.</verify>
  <done>The Browser still feels like part of openOS after the direct-navigation expansion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
- [ ] Local interaction confirms direct entry and fallback behavior work together
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Browser accepts direct URLs and goes beyond the original curated list
- The UI stays coherent and explicit about fallback behavior
</success_criteria>

<output>
After completion, create `.planning/phases/17-browser-direct-navigation/17-02-SUMMARY.md`
</output>
