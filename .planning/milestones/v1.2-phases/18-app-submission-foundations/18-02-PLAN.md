---
phase: 18-app-submission-foundations
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - scripts/validate-submitted-apps.ts
  - package.json
autonomous: true
requirements:
  - PLAT-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 18-2026-04-11T18-46-52
generated_at: 2026-04-11T18:46:52.914Z
must_haves:
  truths:
    - Submission metadata is validated through a repo-owned command instead of manual review only.
    - Validation fails clearly when required submission metadata is missing or inconsistent.
    - The validation workflow is lightweight enough to run in local contributor loops and future CI.
  artifacts:
    - A checked-in submission validation script exists.
    - `package.json` exposes a clear repo-native submission validation command.
    - Validation output is actionable for contributors.
  key_links:
    - The validation command consumes the same shared submission metadata module created in Plan 01.
    - The submission workflow stays repo-driven and does not depend on any external service or marketplace backend.
    - Future catalog work can trust that `catalog-ready` submissions have already passed repo validation.
---

<objective>
Add the repo-owned submission validation workflow.

Purpose: Turn the submitted app metadata contract into a contributor-facing repo workflow that can fail fast on invalid submission metadata before it is treated as catalog-ready.
Output: Submission validation script and repo-native validation command.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-app-submission-foundations/18-CONTEXT.md
@src/features/platform/submittedAppManifests.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-owned submission validation script</name>
  <files>scripts/validate-submitted-apps.ts</files>
  <action>Create a rerunnable repo-owned script that validates all submitted app manifests, prints actionable output, and exits non-zero when invalid metadata would block catalog-ready review.</action>
  <verify>`bun run submissions:check` passes with the sample submission in place.</verify>
  <done>The submission review workflow is executable instead of being docs-only.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the validation entrypoint through package scripts</name>
  <files>package.json</files>
  <action>Add a repo-native command for submission validation so contributors and future CI can run the same workflow consistently.</action>
  <verify>`bun run submissions:check` is available and green.</verify>
  <done>The repo now has a clear validation entrypoint for submission metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the validation output contributor-friendly</name>
  <files>scripts/validate-submitted-apps.ts</files>
  <action>Ensure validation output names the broken manifest and the failing field/message so a contributor can fix the issue without reading implementation code.</action>
  <verify>The validator output is readable and deterministic when run locally.</verify>
  <done>The submission review workflow is practical to use from the command line.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run submissions:check` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-owned submission validation exists and is easy to run
- Invalid submission metadata would fail fast before catalog-ready use
</success_criteria>

<output>
After completion, create `.planning/phases/18-app-submission-foundations/18-02-SUMMARY.md`
</output>
