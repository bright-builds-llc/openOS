---
phase: 04-motion-and-app-navigation
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/features/motion/HomePill.tsx
  - src/features/runtime/AppSurface.tsx
  - src/features/runtime/runtimeShell.css
  - src/features/motion/MotionLayer.tsx
  - src/features/motion/motionNavigation.css
  - src/features/shell/AdaptiveShellFoundation.tsx
autonomous: true
requirements:
  - MOTN-02
  - MOTN-03
user_setup: []
must_haves:
  truths:
    - The Home pill appears only while an app is open.
    - Tapping the Home pill reverses back to the home screen through the shared transition system rather than a hard cut.
    - Reduced-motion mode uses a much gentler close transition while preserving the same close behavior.
  artifacts:
    - A shared Home pill component exists.
    - Shared app-surface chrome integrates the Home pill without app-specific duplication.
    - Close-transition styling exists in the motion stylesheet.
  key_links:
    - `AppSurface` renders the Home pill only while an app is open.
    - The Home pill triggers the same motion/navigation controller used for open transitions.
    - Close transitions return to the home screen through the shared motion layer.
---

<objective>
Implement the Home pill and reversible close transition back to the home screen.

Purpose: Complete the navigation loop so apps open and close through one shared motion system.
Output: Shared Home pill component, close transition behavior, and reduced-motion close fallback.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-motion-and-app-navigation/04-CONTEXT.md
@.planning/phases/04-motion-and-app-navigation/04-RESEARCH.md
@src/features/runtime/AppSurface.tsx
@src/features/shell/AdaptiveShellFoundation.tsx
@src/features/motion/MotionLayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared Home pill component</name>
  <files>src/features/motion/HomePill.tsx, src/features/runtime/AppSurface.tsx, src/features/runtime/runtimeShell.css</files>
  <action>Create a small centered glass-like Home pill that appears only while an app is open. It should sit above the bottom safe area, feel secondary to app content, and be mounted from the shared app surface rather than from app-specific content.</action>
  <verify>`npx tsc --noEmit` and `pnpm build` pass.</verify>
  <done>The Home pill exists as shared shell chrome, not app-specific UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire reverse-to-home close behavior through the shared motion layer</name>
  <files>src/features/motion/MotionLayer.tsx, src/features/shell/AdaptiveShellFoundation.tsx, src/features/runtime/AppSurface.tsx</files>
  <action>Make Home-pill taps drive the motion/navigation controller so the app surface reverses back into the home shell rather than instantly disappearing. Preserve the same outer container system used by app opening.</action>
  <verify>Local shell check confirms Home-pill taps animate back to home rather than hard-cutting; then `pnpm build` passes.</verify>
  <done>Open and close both use one reversible motion system.</done>
</task>

<task type="auto">
  <name>Task 3: Add reduced-motion close behavior and final transition polish</name>
  <files>src/features/motion/motionNavigation.css, src/features/motion/MotionLayer.tsx, src/features/runtime/runtimeShell.css</files>
  <action>Implement the gentler reduced-motion close behavior and final polish on timing/easing so open and close feel like one calm system. Keep the reduced-motion path visually softer but semantically identical.</action>
  <verify>`pnpm build` passes and both normal and reduced-motion close paths are represented in code/CSS.</verify>
  <done>The close path is polished, reversible, and reduced-motion-aware.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `pnpm build` passes
- [ ] Local shell check confirms the Home pill appears only while an app is open and closes back to home through the shared motion system
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Home pill is visually restrained and shell-native
- App closing feels like the reverse of app opening instead of a hard reset
</success_criteria>

<output>
After completion, create `.planning/phases/04-motion-and-app-navigation/04-03-SUMMARY.md`
</output>
