---
phase: 20-verification-and-distribution-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-v1.2.sh
  - package.json
  - README.md
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-04-11T19-00-30
generated_at: 2026-04-11T19:00:30.974Z
must_haves:
  truths:
    - The repo exposes one canonical command that proves the current milestone quality bar.
    - The command covers submission validation plus the full launcher-path verification surface.
    - Contributors can discover that command from the normal repo entrypoints.
  artifacts:
    - A repo-owned `v1.2` verification script exists.
    - `package.json` exposes the verification entrypoint.
    - README quickstart or verification guidance points to the new command.
  key_links:
    - The verification entrypoint reuses the existing submission validator and repo test/build commands rather than duplicating logic elsewhere.
    - Future milestone closeout can use the same command as evidence.
    - The final phase stays verification-first instead of sneaking in product work.
---

<objective>
Add the repo-native `v1.2` verification entrypoint.

Purpose: Give the milestone one canonical verification command that proves the current submission, catalog, Notes, Browser, and shell regression bar together.
Output: Verification script, package command, and minimal discoverability wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-verification-and-distribution-integration/20-CONTEXT.md
@package.json
@scripts/validate-submitted-apps.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a repo-owned v1.2 verification script</name>
  <files>scripts/verify-v1.2.sh</files>
  <action>Create a small rerunnable script that runs submission validation, tests, typecheck, build, and the full launcher-path WebKit suite in the intended milestone-closeout order.</action>
  <verify>`bash scripts/verify-v1.2.sh` succeeds on a clean phase result.</verify>
  <done>The repo has one canonical verification script for the current milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the verification entrypoint through package scripts</name>
  <files>package.json</files>
  <action>Add a repo-native script entry so contributors and future CI can run the same final verification command consistently.</action>
  <verify>`bun run verify:v1.2` is available and green.</verify>
  <done>The final milestone verification has an obvious repo command surface.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the final verification path for contributors</name>
  <files>README.md</files>
  <action>Add a minimal README pointer to the new verification entrypoint so the final quality bar is discoverable without reading planning files.</action>
  <verify>The README references the repo-native verification command.</verify>
  <done>The milestone verification path is visible from the main repo entrypoint.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run verify:v1.2` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo exposes one canonical `v1.2` verification entrypoint
- Contributors can discover and run it easily
</success_criteria>

<output>
After completion, create `.planning/phases/20-verification-and-distribution-integration/20-01-SUMMARY.md`
</output>
