---
phase: 16-notes-search-and-organization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/apps/notes/notesModel.ts
  - src/features/apps/notes/notesModel.test.ts
  - src/features/apps/notes/notesStorage.ts
  - src/features/apps/notes/notesStorage.test.ts
autonomous: true
requirements:
  - NOTE-05
  - NOTE-06
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-04-11T15-41-23
generated_at: 2026-04-11T15:41:23.614Z
must_haves:
  truths:
    - Notes search and organization state are modeled in the Notes domain and repository layer, not improvised only in component state.
    - Existing flat-note local data upgrades safely into the new organization model.
    - Search semantics stay deterministic and testable without rendering the UI.
  artifacts:
    - The Notes domain model includes folder-aware note data and focused search helpers.
    - The Notes repository persists folders and folder assignment in a backward-compatible local snapshot.
    - Focused unit coverage exists for search and storage migration behavior.
  key_links:
    - The upgraded Notes data layer still hangs off the canonical runtime/platform storage namespace.
    - Later Notes UI work can consume folder/search behavior from repo-owned helpers instead of owning raw localStorage logic.
    - Existing local Notes data is preserved through the new storage shape.
---

<objective>
Build the folder-aware Notes domain and persistence layer.

Purpose: Establish a durable search and organization model before reshaping the Notes UI, while preserving the current local-only Notes data already stored in browsers.
Output: Folder-aware Notes model, storage migration path, and focused unit coverage for search and persistence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-notes-search-and-organization/16-CONTEXT.md
@src/features/apps/notes/notesModel.ts
@src/features/apps/notes/notesStorage.ts
@src/features/runtime/appRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the Notes domain model for folders and search</name>
  <files>src/features/apps/notes/notesModel.ts, src/features/apps/notes/notesModel.test.ts</files>
  <action>Add the smallest folder-aware Notes model that can support search, folder browsing, and current note list rendering. Keep helpers deterministic and centered on visible behavior.</action>
  <verify>`bun run test -- src/features/apps/notes/notesModel.test.ts` passes.</verify>
  <done>Notes has a stable model for folder-aware search and list rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade local persistence with backward-compatible migration</name>
  <files>src/features/apps/notes/notesStorage.ts, src/features/apps/notes/notesStorage.test.ts</files>
  <action>Persist folders and folder assignment through the existing Notes storage namespace, and migrate legacy flat note payloads into a default folder without losing current local data.</action>
  <verify>`bun run test -- src/features/apps/notes/notesStorage.test.ts` passes.</verify>
  <done>The Notes repository can persist search/organization state without breaking existing Notes data.</done>
</task>

<task type="auto">
  <name>Task 3: Lock unit coverage around migration and search behavior</name>
  <files>src/features/apps/notes/notesModel.test.ts, src/features/apps/notes/notesStorage.test.ts</files>
  <action>Add focused unit coverage for folder sorting, search filtering, duplicate-folder protection, and legacy-data migration. Keep tests centered on behavior instead of storage implementation details.</action>
  <verify>`bun run test` and `bun x tsc --noEmit` pass after the repository/model changes.</verify>
  <done>The upgraded Notes data layer is regression-resistant before UI work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test` passes
- [ ] `bun x tsc --noEmit` passes
- [ ] `bun run build` passes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Notes data survives upgrade from the flat-note model
- Search and folder semantics are testable without rendering the app
</success_criteria>

<output>
After completion, create `.planning/phases/16-notes-search-and-organization/16-01-SUMMARY.md`
</output>
